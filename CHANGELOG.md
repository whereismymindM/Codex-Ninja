# Changelog

> **怎么写**：先 git log 确认当前最新版本号，再往上加。新改动加在最上面，不是接在底部。写完一批改动后，在改动块上方居中标版本号。未提交的标 (待提交)，提交后换成版本号。大版本叫「初代」「二代」，小版本叫「初代 --v1.0.1」「初代 --v1.0.2」……依次递增。
> **格式**：每条 - 开头，写改了什么（为什么/影响范围）。同一天的多条按时间从上到下排。简洁，人读。
> **实测记录**：实弹测试必须记「测试基线」——哪个分支、哪个版本/提交、测了什么、结论。下次测试先对照基线判断 Skill 有无变化。

---

## 2026-08-11 试用轮 TRIAL 输出 + run_e2e_trial.sh（Skill 自举实弹落地 · 四阶段模板首跑）

- **monitor.js 新增 `模式: 试用` 识别（D组#10）**：输出 `TRIAL N=x (等真人反馈: 试用反馈.md 未就位)` 与待命轮 STANDBY 区分（此前语义混淆，大鱼无法区分"试用轮等真人" vs "待命轮等追加"）；等真人反馈阶段**豁免 WAIT_OVERDUE 熔断**（等真人点程序是预期长等待，产出未就位 ≠ 卡轮）；反馈文件就位 → WAIT 摘要带 `试用反馈已到，角色处理中` 原因；改动限定试用轮分支（4 处：B 变量提升/A 识别+反馈探测+三分支/C WAIT 摘要/D 熔断豁免）
- **新增 `e2e/run_e2e_trial.sh`（D组#11）**：试用轮协议 T1-T7 用例（等反馈 TRIAL/反馈切换/产出推进/熔断豁免与恢复/待命与收工回归护栏），独立脚本不扰动既有三脚本，9 断言
- **来源**：四阶段流程模板首次实弹（Skill 自举项目，档案 22_）：拆缝(架构师方案 v2)→测试基线(图灵)→做肉(DHH 工作副本+契约符合性声明三级证据)→拼缝(副本 8/8 场景+分界声明)→试用(老渣落地+真人验收)→收工(3/3 退场)；**角色独立发现"A4 产出就位 → WAIT N+1 系既有自检推进机制非 DONE"**（对照未改动版排除回归，以实测行为为准）
- **验证**：run_e2e_trial.sh 9/9 + 既有 e2e 52/52 全绿（总断言 61）；落地后实战（005 试用轮）输出 TRIAL 生效
- 观察项：monitor STUCK 误报现场（角色写长文档 14 分钟无心跳被误报"挂死"，自我恢复）——与隐患#19 同源，心跳判据边界待后续

## 2026-08-11 四阶段流程模板 + compose 状态推导缺陷修复 · 2026-08-11（feat e485c3f）

- **新增 `scripts/templates/四阶段流程.json`**（7 轮：拆缝(主笔审核)→测试基线先行(单人输出)→做肉×2(单人输出)→拼缝(主笔审核)→试用(试用轮)→收工；7 占位角色）——Solo×Skill 组合开发流程规范-定稿 v1.0（013 轮组团评审）落地为可执行剧本，四阶段编排操作手册「待办」闭环；来源=定稿非档案自证
- **compose.js 状态推导缺陷修复**：任务轮"未提及角色"误用上轮当前状态（活跃）→ 改用上轮本轮后（待命/休眠）——补四阶段模板时暴露：拆缝后架构师/产品经理在后续轮误显"状态：活跃"，角色会误以为自己要干活（四阶段每轮仅部分角色活跃，暴露明显；接力型模板角色重叠高此前未暴露）；prevAfter 定义前置
- templates/README.md 清单加四阶段行（模板库 5→6）
- 验证：node --check OK + 四阶段替换占位符生成 7 张公告牌状态矩阵完全正确（每轮仅活跃角色显活跃）+ 5 个旧模板回归生成全通过 + 流转校验编译期对齐

## 2026-08-11 排版补拆 4 处超长行 · 2026-08-11（三方视角审查批 8f4de62 的排版复核续）

- 排版结构复核（对照 08-09 排版审核口径：分隔线/表格/围栏/超长行/引用/全角半角）发现 4 处超长行 → 拆分：启动指南大鱼 toml 警示 729 字节 → 3 行（引用块句号处断行，`>` 前缀连续）；完全指南定模式表试用行 / 团队须知试用轮行（表格单元格精简措辞，删冗余动词，语义不变）；CHANGELOG b7fc4bc 条目（分号处断行缩进续行）
- 复核结论：围栏全配对 / 表格行全完整 / 引用 0 悬空 / 新增行 0 全角 / 超长行（字符数口径）0——测量口径纠偏：awk 按字节数误报 36 处（中文 3 字节/字），按字符数实际仅启动指南 1 处真超限
- 验证：字符数重扫 4 处全 <200（117/149/170/110/102/131）；纯文档零逻辑，不需 e2e

## 2026-08-11 三方视角审查修复 11 项（追记于 12-25 之上的补记批次内，详见 git log 0a51909 后）

- 试用轮同步漂移修复：完全指南模式枚举补「试用」（6→7 值）+ 定模式表加试用行；团队须知轮次类型表补试用轮（三种→四种）；SKILL 参考表"六种轮型"→"七种"（试用轮 08-11 上线时 4 处同步、3 处漏）
- **compose.js 校验缺口修复**：`needOutput` 加「试用」——试用轮漏写产出负责人/产出/任务目录从"编译期不报错"变"报错拦截"（原缺口 → monitor 活跃轮 WAIT 卡死）
- **monitor.js 判据自洽修复**：收工轮 hbForce 补 A-1 产出复核——心跳 stale 但对讲目录窗口内有新文件（写流水账/角色记忆/退场文件中）不再强制视为已退场，与主心跳检测区"心跳不能单独作为死亡证据"判据一致（原窗口常驻 2 分钟阈值会误判收工轮写长文档角色提前 DONE）
- 启动指南补大鱼 reasonix.toml 旧版警示：2026-08-05 前生成的缺 `allow_write = [火影-大鱼]`，fish 重建不更新已有 toml，修复=删 toml 重跑 fish
- SKILL 参考表补索引：compose.js+templates（编排自动化）/老渣文档 README/通用公告牌/Multi-pass/goal模式认知；步 4 提 compose 可选用法
- CHANGELOG 补记 2026-08-07~08-11（本批次，见下）

## 补记 2026-08-07 ~ 08-11（第 13/14 轮 + 信号协议加固 + goal 模式 + 编排自动化 + 试用轮）· 2026-08-11 追记

> CHANGELOG 此前停在 12-25（08-06）；本批 60+ 提交按日期+主题追记，细节见 git log。

### 2026-08-07（收工体系重构）
- 收工三件套→两件套：大鱼收口只写 产出总结.md（逐轮完成矩阵，成果硬证据）+ 项目完成.md（一句话通知）；删审计报告_外部观测.md——推进节奏/时间线全在 _fish_loop.log+监控日志 自动记录，大鱼复述=浪费 token（数据采集与汇报分离）
- 收工后老渣待办体系 + 调查模板：SKILL 新增第 6 步（查节奏/核产出/时序校验/发大鱼调查/归档/迭代，主动列→问用户→获准才做）；新增 大鱼侧调查.md（收工配套）、协作体验调查.md、时间卡点调查.md
- 大鱼工具手册 + 时序校验.sh：monitor 21 种输出全解+4 工具+排障表（大鱼不用翻源码）；时序校验 = mtime 判定双人抢答/辩论跳步/审核打回异常（老渣待办 #3 工具化）
- _fish_loop.js 对讲目录监控（NEW_TASK 检测老渣任务，防回合间隙任务悬空）+ 时序校验辩论段排序 bug 修复（字典序→mtime 时间序+阶段不倒退）

### 2026-08-08（信号协议 + goal 模式 + 灵魂注入）
- 信号_acked 协议机制化：wait_file --ack 自动替换改名（.signal→.signal_acked，防漏改名/追加式改名）；时序校验.sh 加信号卫生检查（残留 .signal / 追加式 .signal.signal_acked fail-loud）
- 验收轮固化 + 写方缺信号告警：主笔审核签字条件=静态审查+实弹验证双过；wait_file 等待前扫描发现近 5 分钟 .md 缺 .signal 立即告警（隐患#17 前置）
- 缺信号告警强化为失败：wait_file exit 5（漏发从静默变失败）；辩论玩法文件加信号原子两步强制（写 .md 后下一条命令必须发信号）
- goal 模式五连：公告牌约定（警告栏禁标 complete/blocked）→ 警告栏最终版 → 双轨模型落地（信号 .reasonix-done + complete 标注双轨）→ goal模式认知.md 单一事实源 → 按需启用设计（标准模板警告栏改普通可选约束）
- 角色进场自动带代码灵魂：_启动多步曲 第 0 步读 ../.soulforge/project-soul.md（SoulForge 结构地图，读不到跳过）
- monitor 产出深扫 mtime 门控（60s 常驻开销大头，key 存 .monitor_state.json）
- 时序校验两处修复：主笔审核配对保留 _已处理 请审记录（误报'结果先于请审'修复）；辩论终结收敛豁免（存在 辩论_终结.md 且终结晚于上一文件=合法收敛）
- 公告牌标准模板冗余清理（goal 决策说明移出骨架、复用模板清单改指路 README、收工轮特例并入硬标准 5）

### 2026-08-09（第 13 轮：多审核方 + 职责盘点 + 三方视角审核）
- 老渣对讲公告牌模板：通用公告牌新增（老渣=问方写问/角色=答方，模式=单人输出，不列老渣，产出路径写死）+ 简化（产出归答方、对话结束 deliver+sign 收尾）
- 收工轮模板加角色记忆：退场前写 {角色}_角色记忆.md 到对讲目录（给下一任的自己，与流水账区分=记录 vs 传承）+ 触发时机定稿（仅终局轮/中途退场写）
- wait_file 缺信号检测 2 个实战 bug（稻盛和夫 001 实测）：13-3 兼容 xxx.md.signal 协议命名（原只认 xxx.signal 误报）；13-4 跳过归档文件（_第N次/_已处理 改名留痕非新产出）
- 多审核方协议 13-5：≥2 审核方时审核结果文件带角色名（审核结果_{角色}.md），主笔 glob 逐个读（防三方同名覆盖）+ 13-5 补充（主笔必须等齐全部审核方 signal，wait_file 多目标 AND）
- 流水账文件名明示 13-6（明茨伯格 005 质询）：收工轮流水账必须用独立文件 {角色}_流水账.md（不是操作日志）
- wait_file 扫描范围收窄 13-7（泰勒 006 质询）：MISSING_SIGNAL_ABORT 只查等待目标同名 .md，搭档正在写的/别人的产出不再误伤
- 推进门 + 收工职责盘点 13-8（香农事故固化）：当轮任务未完成即使 poll 到新牌也不得推进；收工前盘点公告牌分配任务是否已交付，未尽先补交/求助/说明；收工轮任务栏固化职责确认步骤
- 三方视角审核修复合集 b7fc4bc（21 文件）：老渣视角（收工轮任务文本进模板/团队须知补 add 复制命令/待命写法区分/roles.json 三字段/内部代号清理/SKILL 行为约束节删除/README 分支Tag折叠/完全指南 goal 附录）；
  角色视角（辩论老渣写牌提示越界移除/验收轮与信号原子两步轨迹清理/团队须知全角冒号改半角/外部环境BUG清单重写 11KB→5.5KB）；
  职责盘点约束补齐执行层（_干活流程收工轮节，三层全覆盖）；大鱼视角（唤醒表述统一/monitor 源码口径统一）
- 公告牌契约修复：待命轮模板角色行补'等通知'；AGENTS自检/流程体验报告补产出类型字段+标题去注释（与其他 7 个调查轮模板统一）；8 个调查轮任务路径统一为绝对路径
- 排版结构审核 P0+P1（辩论引用块语义/流水账英文残留/工具速查'独立脚本优先'口径修正/SKILL 5步→6步 等）+ 完全指南排版修复（分隔线统一/超长行拆分）+ 完全指南复核（开发轨迹残留清理）
- 审查清单并入三方视角审核（阅览室/Skill三方视角审核.md 附录八，单一事实源）；隐患清单瘦身（已修复区压缩为摘要表格）；工具速查日期戳更新
- 隐患#19（长写文件触发 DEAD/STUCK 误报边界：深泽直人 005 写 105KB 翻译 4.5 分钟被误判挂死）+ 隐患#20（.signal_ok 事件：林纳斯发信号多写 echo 建成协议外文件，质询定位→自认→工具加固）
- wait_file --ack 支持等 .md 自动 ack 同名 .signal（信号质询 007 根因）；辩论玩法文件新增「信号协议铁律」区块
- 信号协议加固（.signal_ok 质询结论落地）：信号后缀白名单检测（NONSTANDARD_SIGNAL 告警不阻塞）+ ACK 动作留痕（操作日志 [时间] ACK xxx.signal -> xxx.signal_acked，解决归因歧义）

### 2026-08-10（wait_file ack 默认 + 编排自动化 + 组团终审）
- wait_file 自动 ack 改为默认开启（林纳斯 003 手动 rename 留痕黑洞根治）：ackMode 默认 true 无需带 --ack，--no-ack 显式关闭；辩论玩法文件同步'手动 rename 是留痕黑洞'警示
- 辩论第 6 步签字独立于裁判结论：正反方写完总结直接 sign 不再等裁判结论 .ready（林纳斯白等 594s 教训）；裁判掉线由 monitor WAIT_OVERDUE 兜底，正方代接改为 monitor 提示后触发
- 手动 rename 检测告警：wait_file 发现 .signal_acked 存在但操作日志近 5 分钟无对应 ACK 行 → MANUAL_ACK_DETECTED 告警（马斯克 3 处 bash mv 留痕黑洞实证）
- 多审核方请审核信号带角色名（隐患#18 请审核侧修复）：主笔按审核方分别发 请审核_{角色}.md.signal，各审核方等/ack 自己的互不干扰（单审核方保持原样）
- 修复清单 8 项（文档口径一致性）：双人玩法超时代码块 _deadlock 残留清理/完全指南死锁口径按模式分述/_工具速查旧参数清理/monitor 黑名单三处统一 6 字段/失联处理不对称补设计说明/RSS 示例 001 行内注/README 目录树补齐
- 组团默认终审 pass + 裁判审核逐条核验（P0-1/P0-2，模式辩论场辛顿裁判结论缺陷落地）：单人输出默认终审自检；裁判/审核意见升级逐条核验格式（成立/不成立/部分成立+证据引用）
- 灵魂注入摘要优先：角色进场读 project-soul.summary.md（几 KB 带地图）替代啃全文 169KB，成本降 50-60 倍；灵魂锚定改向上查找（CWD 逐级找最近的 .soulforge/，修正容器/项目粒度缺陷）
- **公告牌编排器 compose.js**（编排自动化，唯一纯手工环节工具化）：JSON 配置 → 自动生成公告牌_NNN.md + 状态矩阵 + 流转编译期校验（本轮后≠下轮状态画错发不出）；完全指南「十」补第 7 步
- 流程模板库 v1.1：5 个现实团队流程模板（标准软件交付/技术选型决策/知识挖掘/代码审查/质量审计）+ compose --list；流转校验修正（待命→活跃 允许，只有休眠/退场→活跃 拦截）
- 工具源码快照补 compose.js（B-8 快照清单 + 现场快照手动补）
- 主笔审核 v1 通过即最终通过（隐患#21，18_团队流程卡死 20 分钟根因）：审核方'通过签字'= 职责完成可退场，v2+ 修订为主笔自审不再发三方（谁签得快谁错过的状态机不同步根治）
- 软件开发全流程模板（S1-S8 9 轮 7 角色，18_团队流程现实提炼）：需求定义→需求评审→架构设计→契约对齐→开发实现→测试验收→发布决策→发布复盘+收工；替代早期自证版 标准软件交付.json
- 流程模板库 README（人看索引：5 模板清单+三步用法+JSON 结构+维护规则）

### 2026-08-11（等文件统一 + 试用轮）
- 等文件统一 wait_file.js（DHH 自建 _wait_reviews.js 幽灵目录踩坑根治）：自写 = 把锚定约定交还角色记忆、拼错靠自觉（路径错位+心跳错位+回合闭合三真 bug）；6 处文档统一"等文件一律优先 wait_file（多目标 AND/--any/--watch-hb/自动续心跳/自动 ack 全内置），内联仅当不满足需求时手写"
- 主笔审核审核方第 1 步等请审核改 wait_file.js（5a99c98 遗漏补修，尤雨溪质询实锤：三文档不一致引导角色自建 wait_review8.js）
- 公告牌标准模板加「试用」轮型（Solo×Skill 组合规范定稿 v1.0）：真人实际点一遍交付物→反馈清单→角色修→deliver+sign——试用轮制度化让'真人用'成为正式编排环节（用户实测反复抓出人性化 bug，AI 自审审不出）
- 试用轮补全：compose.js MODES 加「试用」；_公告牌解读.md 模式表加试用行；_干活流程.md 新增「试用轮」节（待命等真人反馈→按反馈修（六态自测）→试用报告→deliver+sign）

---

## 12-25 _fish_loop.js --once 单轮汇总 · 2026-08-05（大鱼工具复盘最卡①）

- `_fish_loop.js` 新增 `--once` 模式：单轮检测汇总后退出（checkBoards + runMonitor 各跑一次）——大鱼每轮轮询从"sleep 55 + tail + ls"三条命令变一条 `node _fish_loop.js --once`（复用现有函数零新增逻辑，原 while 循环路径不动）
- 输出含 12-24 判定摘要（`MONITOR: WAIT N=3 (收工轮 退场 2/3 缺 图灵)`）——一轮汇总直接可决策
- 两版大鱼模板「周期验证」节补 --once 用法（窗口常驻"每个回合醒来"处 / run 拉起"机制"处）
- 实测：--once 单轮退出 0 + 原循环 65s 回归（MONITOR 行正常）；e2e 不涉及（_fish_loop 独立脚本）

## 12-24 monitor 判定摘要 · 2026-08-05（大鱼工具复盘最卡②）

- monitor 的 WAIT/DONE 输出带判定原因——大鱼不用读源码就懂：`WAIT N=1 (产出 1/2 .ready 未就位)` / `WAIT N=1 (收工轮 退场 2/3 缺 角色C)` / `DONE N=1 (回看确认: 收工轮 全员退场 3/3)`（主判定区 + 回看分支双覆盖）
- outputProgress 精确累计（格式 B 各自 = need 活跃数 / have 实际 .ready）；实测 4 场景 + e2e 52/52

## 12-23 陷阱时刻同步到大鱼模板 · 2026-08-05（用户要求：图灵 003 第二类陷阱显式写进大鱼模板）

- 窗口常驻大鱼模板**新增启动铁律 7「表态文本 = 回合结束」**：任何不伴随工具调用的纯文本（"收到""我在盯着""继续轮询"）都是最终回复 = 回合结束——即使内容是"我在在线"；两类陷阱时刻（干完活总结 / 工作间隙表态）全量显式；保活 = 发工具调用（拉日志/查目录/跑 monitor），不是说自己还活着
- 此前只有 :136 收口后禁令（收尾点语境）+ run 拉起版 :199 回合保持铁律（"口头声明=下线的瞬间"）——窗口常驻版日常语境缺显式，本次补齐，两版一致
- 修正 12-22 复核结论中"大鱼侧已全显式无需沉淀"的判断：收口禁令 ≠ 日常表态陷阱，第二类陷阱在大鱼模板同样需要显式（用户纠正）

## 12-22 大鱼审计兜底认知沉淀 · 2026-08-05（大鱼 002 自检④——双日志互为兜底）

- 大鱼侧保活规则复核结论：**已全显式**（启动铁律 4"你≠_fish_loop.js（脚本在跑≠你在场）"、铁律 5"在场=拉日志+做决策"、:136 收口占位话术禁令——与角色"双杀认知/第二类陷阱"同构的知识大鱼模板早已写好，无需沉淀）
- 唯一增量：**审计素材双日志互为兜底**——`监控日志.md` 是辅助流水、`_fish_loop.log` 的 MONITOR 行是主证据；某时段监控日志缺失（写入失败/被清/断档）时收工审计以 `_fish_loop.log` 为准兜底（第十二轮实测 8 分钟断档审计照常）；monitor 侧写入失败已打 MONLOG_WARN（12-14），双日志对不上以 _fish_loop.log 为准

## 12-21 第二类陷阱时刻 · 2026-08-05（图灵 003 复盘补——表态文本也是回合结束）

- 12-20 口诀只覆盖"干完活想总结"（第一类）；图灵补**第二类**：工作间隙想说"我还在/继续轮询/收到"表态——**任何表态文本都是纯文本 = 最终回复 = 回合结束**，即使内容是"我在在线"（角色以为"我还在"是保活动作，实际是终结动作——保活 = 发工具调用，不是说自己还活着）
- 行动口诀扩为"两类陷阱时刻"，待命轮期间角色若想说"继续轮询"→ 只发 poll 工具调用，不发表态文本

## 12-20 回合保活知识沉淀 · 2026-08-05（乔布斯/纳特 003 复盘 C 段——真实执行的保活机制）

- **行动口诀**：「最终回复=回合结束」节补乔布斯/纳特独立验证的口诀——**永远让"下一个动作"是工具调用，不是输出**；"最危险时刻 = 干完活想总结的瞬间"（两人 001/002 干完都被此拦住）
- **双杀认知**：「续心跳」节补——**心跳停 ≠ 回合结束**（poll 还在跑就能被 WOKEN 唤醒）；真正该死的是"回合结束 + 心跳停"双杀。保活的本质是保回合，不是保心跳
- **写长文件边界**：`write_file` 单次工具调用秒级完成不触发心跳断（纳特写 12KB 报告实证）；只有连续长干活（单次调用 >2 分钟）才需"每 30s 写心跳"主动续
- 意义：模板保活机制被两角色独立执行零偏差验证（真实运行证据）；3 条"隐含变显式"沉淀回模板，自检轮闭环完整

## 12-19 纳特 003 复盘修复 · 2026-08-05（双人对话信号卫生 + 心跳契约）

- **T 序号递增防旧信号秒返**：双人玩法问方出题前先扫目录取最大 T、新问 = max+1（禁止凭记忆手写序号——写错旧序号命中残留旧 .signal 秒返、读到旧内容重复回答，第十二轮纳特 4 次等待全靠运气）；问方读答后删 `答.md.signal`、答方读问后删 `问.md.signal`——信号文件从不清理是秒返风险源，读完即删
- **--watch-hb 心跳契约明确**：传 `{问方角色名}_大鱼对讲/_heartbeat.txt`（monitor 判 DEAD 同源文件）；`_mtime.txt` 是 poll 时间戳不参与判死——别传它（传错 = 失联检测与 monitor 不同源）

## 12-18 独立脚本优先 · 2026-08-05（乔布斯/纳特 003 复盘：sign/deliver 内联样板是共同最卡点）

- **sign/deliver/lock 独立脚本优先**：角色模板「Node 工具函数」节改"优先独立脚本"（`node _sign.js <轮次> [消息]` / `node _deliver.js <文件名> <任务目录> [源路径]` / `node _lock.js acquire|release <锁名>`），内联函数降级为 fallback 小节——脚手架早已分发 _sign.js/_deliver.js/_lock.js 但模板 0 处教调用，角色每轮手抄 15-40 行（bash 会话隔离，函数定义不跨会话）
- 铁律 6/7 + 玩法文件（主笔审核/单人输出/双人对话/辩论）+ 公告牌指南共 14 处 `await sign/deliver` 同步为命令形式；残留清零；e2e 52/52
- 含 12-14~12-17（第三轮实测：结束信号语义/格式 B 数量校验/大鱼自检表述/回看竞态重读，详见 git log）

## 12-4 图灵 002 自检 6+1 条修复 + 术语统一 · 2026-08-05

- **角色模板 6 处**（图灵 002 自检报告）：①唤醒路径"当前轮=lastN+1"（原 `完成_<lastN>` 条件恒真/恒假 + 重演 10-5 跳号）②收工触发统一"读到 模式: 收工 即建退场文件"（消除与收工轮节双轨互斥）③依赖等待前置"基线检查 ①-③ 非可选项"④"不要用 while"限定为"干活轮等文件场景"（while 骨架必须）⑤新增「公告牌字段↔动作」对照表（模式: 待命/状态：待命等价、活跃+本轮后、字段缺失默认活跃）⑥收工轮"状态=退场 是 模式: 收工 推导结果"（公告牌不写退场字样）
- **灵魂补全**：roles.json 图灵 background 补"计算观"维度（图灵自检指出四维度缺计算展开；重建角色生效）
- **术语统一**：主称 = "玩法文件"，指南模式枚举处加定义（= assets/玩法模式/ 4 个模式文件，scaffold 复制到角色目录）；README 目录树同步——消除"玩法模式/玩法文件/模式文件"三种叫法混用（大鱼校验时曾困惑"玩法文件"指什么）
- 影响面：模板 6 处均为文字精度修正；e2e 不涉及（纯文档/模板）

## 12-3 实测驱动修复 · 2026-08-05（双人对话 --any + 收工轮任务可省）

- **双人对话玩法文件**：答方等"下一问或结束"必须带 `wait_file.js --any`（第十二轮实测纳特卡 20 分钟：第一问已就位却等不到——AND 语义缺任一；与辩论 10-3 同源）
- **收工轮任务可省**（用户意志）：字段表「任务」+ 大鱼模板×2 校验项 2 同步"收工轮任务可省略（角色模板收工轮节有完整退场步骤）"
- 审查清单补"双文件等信号缺 --any"条目

## 12-2 辩论轮数改可选 · 2026-08-05（用户明确意志：双人对话/辩论自由发挥，不规定轮数）

- **公告牌完全指南**：字段表「辩论轮数」辩论模式必填 → **可选**（不写 = 自由发挥，任一方写 `辩论_终结.md` 即收敛；写 = 设上限）；完整示例加"可选"注；常见错误 #7 反转（不写不是错误，写错不会卡死）
- **玩法模式/_辩论模式.md**：第5步"公告牌指定 N 轮" → "可选指定上限，不指定=自由发挥"；收敛机制统一为 `辩论_终结.md`（提前终结），"轮数满了自动进第6步"限定为"公告牌写了上限时"
- **设计文档**：终止条件/时序图/字段表/审核清单 4 处同步（自由发挥为主，轮数仅上限）
- 双人对话玩法文件本就合规（"问够三轮就交差"是反例 + "连续两轮说不出新东西"自然收敛），未动
- 用户意志：辩论/双人对话**机动性强、自由发挥优先**，轮数只是可选上限

## 12-1 产出格式 fail-loud 防线 · 2026-08-05（第十二轮 005 公告牌产出行占位符事故）

- **monitor 产出占位符报警（代码 fail-loud）**：产出路径含 `{}`（如 `{角色}_报告.md`）→ 打印 `OUTPUT-FORMAT ⚠️ 占位符永不匹配`（自检 + 主流程两处同步）——此前静默 WAIT 卡死，角色/大鱼看不出是写牌问题
- **发布校验 5 项 → 6 项**（大鱼模板×2 同步）：新增「产出格式」——产出行不得含 `{}` 占位符；必须为「具体文件名（含扩展名）」或「尾斜杠目录」二选一；多角色共产出用格式 B（目录）
- **审查清单**：第一类第 3 子类（公告牌写法错误）补"产出行含 `{}`"条目
- **生产公告牌 005 修正**（源 + 发布版）：产出行 `{角色}_自检报告.md` → 格式 B 目录 `产出/任务005_AGENTS自检/`；004 警告栏笔误 `10-9` → `11-1`
- 实测：占位符场景 OUTPUT-FORMAT 报警 ×2、格式 B 目录 + 真实名 .ready 判定完成、e2e 52/52 全绿

## 四·6 自检轮新增 · 2026-08-04（公告牌完全指南）

- **自检轮（AGENTS.md 自检）**：公告牌指南新增四·6 节——改模板前可选加一轮，让每个角色通读自己的 AGENTS.md，挑三类问题（①表述不清 ②矛盾 ③行为约束需加固），每条带原文引用+问题类型+建议改法；产出 `{角色}_AGENTS自检报告.md` 到 产出/任务NNN_AGENTS自检/；收集后分类合并→据此改模板→下一轮角色重建生效。与流程体验报告互补（自检审文档语义，体验审流程体验）

## 11-3 复核修正 · 2026-08-05（11-2 复核结论：deliver roleName 推导方式多角色缺陷）

- **deliver roleName 推导修正**（复核者））：原 readdirSync(我的世界).filter(...)[0] 多角色项目取排序第一个目录（实测"测试甲调用却推导出测试乙"）→ producer 审计链污染；改 `path.basename(process.cwd())`（CWD=角色目录，scaffold 用角色名建目录，11-1 CWD 铁律）——一行级修正，多角色正确
- **wait_file 唤醒检测后 exit 0**（复核者低危）：原检测到唤醒只提示不退出（继续等原目标到超时）→ 角色困惑"提示了但脚本还在跑"；改检测到唤醒即 ack + exit 0，调用方决定下一步
- 复核结论：②③④ 正确、③ 判断成立、e2e 52/52 无回归；① 修正后 11-2 完全闭环

## 11-2 复盘修复 · 2026-08-05（第十一轮复盘：图灵抓 deliver bug + 大鱼抓唤醒盲区）

- **deliver 模板 roleName 未定义 bug（图灵 005 自检第 8 条）**：deliver 函数内引用未声明 roleName → ReferenceError 被 catch 吞 → .ready 缺 producer 字段（三字段证据链丢一个）；修复：从 `{角色}_大鱼对讲` 目录名推导 roleName
- **wait_file 唤醒盲区（大鱼第十一轮审计）**：等待中的角色不跑 poll 循环收不到 _wakeup.md，只能等 20 分钟超时（第十一轮三方互等 20 分钟，真正解卡是超时兜底非唤醒信号）；修复：wait_file 循环内检测对讲目录 _wakeup.md → 存在则 ack + 打印提示
- **NEW_BOARD 重复刷屏**：确认为多实例问题（自造脚本/重复启动 _fish_loop），工具 knownBoards 已去重，非代码 bug——9-10 监控唯一禁令覆盖，不修
- 来源：三方 + 大鱼复盘（乔布斯"超时≠掉线"最佳实践 / 纳特"流程衔接重读玩法文件" / 图灵 deliver bug / 大鱼唤醒盲区）

## 11-1 自检报告优化批 · 2026-08-05（第十一轮 005 自检报告×3，v1.1.0 后首个优化）

- **Skill 模板表述修正**：①poll 传参占位符 `<当前N>` → `<lastN>` 统一（4 处，10-5 改了语义占位符没跟上）②"不要用 while"vs"while 循环模板"矛盾澄清（干活轮 vs 待命轮场景区分，3 人同提）③一次性任务模式衔接"任务完成≠下线"铁律（例外仅此一种）④铁律1 CWD 规则分类（wait_file 已 __dirname 锚定 vs 其余工具须角色目录 CWD）⑤孤立行号引用 ":144" → 具名引用（跨文档行号必漂移）⑥"已全部并入"vs 括号标注并存 → 明确"括号=修正轨迹注记"⑦重启禁令：lastN 从自检重算，禁止回传上次会话值⑧收工轮"不再等"vs 待命轮"继续等"边界澄清
- **辩论玩法文件先手强化**：自由辩论正方先手加粗+补实测教训（第十一轮纳特仍误等反方卡 10 分钟——玩法文件 :139 已有先手规则但角色没执行，属角色行为类，强化提示）
- 来源：第十一轮 005 三份自检报告（纳特 A1-A4/乔布斯 9 条/图灵 10 条）

## 审查清单沉淀 · 2026-08-05（用户建议：易错点记下来防遗忘）

- **新增 `审查清单.md`**（codex-ninja 根）：历轮实测/复核沉淀的易错点，7 大类（文档vs代码一致性/传参记账/路径目录/权限边界/回合下线/等文件/其他高频）——审查 Skill 前逐项过，防遗漏
- **收录已踩坑**：--loop 语义、--any 指引缺失、全角半角失配、poll off-by-one、CWD 偏移、mtime 陷阱、角色越权、最终回复判据、fail-loud 命名等 20+ 条
- 新增易错点记录区：审查时发现新问题 → 追加记录

## 10-8 模式冒号全角实例清零 · 2026-08-05（10-7 复核：①只做了一半）

- **:117「唯一合法退出：收工轮（模式：收工）」+ :152「只有公告牌写模式：收工才是真终点」两处全角实例 → 半角 `模式: 收工`**——10-7 只加了说明没改实例，同文件矛盾未真正消除（复核者抓到"声称消矛盾实际残留"）
- 全角 `模式：收工` 残留 = 0，半角 `模式: 收工` = 4 处统一

## 10-7 复核残留补齐 · 2026-08-05（10-x 全批复核，5 条）

- ①3 **模式冒号统一**：:146 "模式: 收工"补半角标准写法说明（与 :144 一致，消同文件自相矛盾）
- ③2 **求助去越权**：删"在公告牌警告区标注求助未回复"（公告牌由大鱼维护，角色无写权限）→ 改"重写求助"
- ③3 **删除对象统一**：临时脚本删除申请对象"老渣"→"大鱼（走求助通道大鱼对话_NNN.md）"（与求助节一致）
- **8-3 可见标注**：铁律区补"8-3 自检轮修正已全部并入本文"标注（角色检索"8-3"字面找不到——乔布斯自查存疑）
- **轮询方式判据**（图灵）：普通轮=poll一次 / 待命休眠轮=bash while+--standby 或 --loop 60 / 长干活=每30s心跳——--loop 只是性能优化非在场必需
- 复核确认：10-x 全批主体修复正确无新引入缺陷；以上 5 条补齐后"10-x 全批"可标完成

## 10-6 自检轮 30+ 条系统性修复 · 2026-08-05（第十轮 005 自检报告×3）

- **纳特 11 / 乔布斯 9 / 图灵 11 条去重后 ~13 处**：
  ①--loop N 语义修正（图灵2：文档写"当前轮号"实际是"循环次数"——真 bug）②待命"最长20分钟"→"20分钟是常见值非上限"（3人同提）③结束标记示例补"必须角色名前缀"（3人同提）④"等文件内联轮询"悬空指涉→指向 wait_file.js ⑤__dirname 旧铁律作废（wait_file 已锚定，自写脚本才用 cwd）⑥一次性任务模式与 10-1 结束标记衔接（干完即退不触发 END_MARKER）⑦"最终回复=回合结束"补可执行判据（纯文本无工具调用=最终回复）⑧9-6"回应后立即"量化（回应后必须跟工具调用）⑨收工简写补流水账步骤 ⑩铁律4/5/9 统一（判定条件式+原生write_file，消 .tmp 矛盾）⑪警告栏纳入 poll 步骤 ⑫（纳特3.1 wait_file --any 已在 10-3 修）
- 来源：005 自检轮三份报告（全带原文引用+建议改法），质量再创新高（抓到 --loop 真 bug）

## 10-5 poll 传参 off-by-one 修复 · 2026-08-05（第十轮大鱼建议 + 乔布斯跳号根因）

- **角色模板 poll 传参语义明确**：`node _reasonix_poll.js <lastN>`——**lastN = 上一张已处理公告牌编号**（SIGN 002 后传 2），不是"下一张"；poll 内部检测 `公告牌_(lastN+1)`
- **根因**：模板"干完 N+1 继续循环"+"poll <当前N>"表述歧义——乔布斯 SIGN N=2 后传 lastN=3 → poll 检测 004 → **跳过 003 审核**（第十轮死锁源头之一）；纳特同款漂移
- **来源**：大鱼全程总结建议（"乔布斯跳号根因疑似 _reasonix_poll.js lastN 记账 off-by-one"）

## 10-3 辩论玩法文件补 --any 指引 · 2026-08-05（第十轮纳特实测踩坑）

- **辩论玩法文件双文件轮询节补 `--any` 用法**：优先 `node 临时脚本/wait_file.js <搭档.signal> <辩论_终结.md> --any`——**必须带 --any**（不带 = AND 语义两个都等，终结没有时卡死）；手写内联循环保留为兜底
- **根因**：9-4 半成品——wait_file.js 支持了 --any，但玩法文件调用示例没更新（文档落后于代码的又一样本）；第十轮纳特双文件等【反方立论 signal + 终结】未带 --any 卡 10 分钟（反方立论早已就位）
- 现场已人工引导纳特读 02 立论继续；玩法文件修复待第十轮 DONE 统一提交

## 10-2 结束标记命名约定对齐 · 2026-08-05（10-1 复核发现）

- **角色模板结束标记命名修正**：`_结束回合_<当前N>.md` → `_结束回合_<NNN>.md`（**NNN = 3 位补零**，与 已退场_NNN/完成_NNN 统一）——代码用 padStart(3,"0") 检测，模板写不补零会导致角色按指引创建的标记永不匹配 → fail-loud 静默失效（实测：不补零 0 次报警 / 补零 1 次报警）
- **性质**：文档落后于代码的又一样本（同 H-1 迁移提示/启动指南同类）——9-9 乔布斯洞察的微观注脚：约束只在文档文字里，会在表述含糊处失效；代码已锁死约定，模板没同步锁死表述

## 10-1 终极方案：非收工轮结束标记检测 fail-loud · 2026-08-05（乔布斯洞察 + 复核者共识落地）

- **_reasonix_poll.js 加 checkEndMarker()**：角色在【非收工轮】想结束回合必须先创建 `{角色}_结束回合_{N}.md`（对讲目录）→ poll 检测到 = 违规结束回合，报警 `END_MARKER_VIOLATION`（不阻断，交 monitor/人工判定）；收工轮正常退场不创建此标记（退场文件=合法信号），不误报。快路径/慢路径都检测
- **角色模板补铁律**：非收工轮结束回合 = 违规（机制强制），确需提前结束须先创建结束标记；"不再靠自觉，检测会报警"
- **验证**：非收工轮（003）+ 标记 → 报警 ✅；收工轮（006）+ 标记 → 不报警 ✅
- **意义**：从"靠模板文字约束"（冷知识）到"靠机制强制"（热约束）的关键一跃——复核者指出这是系统从"很好"到"机制化自愈"的最后一段路

## 9-11 外部复核 N-1~N-3 修复 · 2026-08-05（0aa8c23 全方位复核）

- **N-1** _reasonix_poll.js WOKEN 铁律提示重复 2 遍 + 缩进错位（9-7/9-9 合并插入重复）→ 去重 + 修缩进；**顺带补齐 9-9 静默失败遗漏**：BULLETIN（新牌就位）提示原本 replace 的 NEW_BOARD 字符串不存在没生效，现已补到三处 BULLETIN（信号路径/快路径/慢路径）
- **N-2** monitor.js WAIT 分支 logMonitor 调用 2 次（复制粘贴重复，待命期日志膨胀一倍）→ 去重
- **N-3** _reasonix_poll.js --standby 硬编码 args[2]（与 --loop 组合顺序敏感）→ 改 args.indexOf 匹配
- 复核确认：wait_file 锚定/--any、_fish_loop 零决策、monitor A-1/STUCK/.acked、角色模板全批、scaffold/_deliver 全部正确；e2e run_e2e.sh 14/14 绿（multi/sys 未跑，建议后续全量）

## 9-10 大鱼自检 10 处落地 · 2026-08-05（第九轮 007 大鱼自检报告 → 大鱼模板）

- **大鱼模板（窗口常驻+run拉起）按自检报告落地**：①监控工具唯一性禁令（禁自造/叠加 watch 脚本替代 _fish_loop.js——第九轮自造 _fish_watch.sh 致双 monitor+日志双份）②"第一件事"统一（看牌优先，删开工仪式"第一件事"字样）③20 分钟补搬基准三处统一"前一轮完成时刻+20 分钟"（待命轮发布/就位不参与计算，公告牌 mtime=写牌时间不可靠）+ 补搬执行确认（到点±1分钟+角色poll/退场/DONE三确认）④收尾点补"非收尾点"限定 + 3 步顺序（DONE→三件套→退场文件→输出，禁占位话术）⑤收工节报告语言明确（产出总结/审计报告英文、项目完成中文）⑥待命轮公告牌 mtime 明确取 我的世界/ 已发布那份 ⑦≤65s 短 sleep 含义明确（等首个日志输出）⑧铁律2 文件操作外延明确（内容读写用 Node/专用工具，复制发布可用 cp）⑨干预阶梯（心跳正常但 5-10 分钟无推进→写 _wakeup.md 提示）⑩唤醒信号闭环（写后记录/10分钟复查补写/收口清理残留）
- 来源：第九轮 007 大鱼_AGENTS自检报告（75 行，全带原文引用+建议改法）

## 9-9 提醒机制修正 · 2026-08-05（乔布斯产品建议，推翻 9-7 刷屏版）

- **撤销 9-7 的 TIMEOUT 每次提醒**（乔布斯指出硬伤：待命轮一次 wait 60+ 行 TIMEOUT，一行警告混进刷屏流=被淹没，与"十几条最高铁律"同款稀释）
- **改关键节点强提示**：_reasonix_poll.js 只在新牌就位/收工轮/唤醒三种输出带"【铁律】最终回复=回合结束——未到收工轮前禁止输出最终回复"，TIMEOUT 不带（防稀释）
- **AGENTS.md 显式声明"最终回复=回合结束"等价关系**：输出任何最终回复=回合结束=下线，不管内容是什么；保持在场=回合内持续工具调用，不是表态（乔布斯：很多角色不知道这条，被催的根源就是它）
- **乔布斯洞察**（记录）：人工指令有效 = "上下文常驻 + 实弹惩罚绑定"，非"记住"；AGENTS.md 输在"常驻平铺+全是最高级"；脚本提示能缓解替代不了——终极方案=非收工轮结束回合须创建标记文件 + poll 检测报警（fail-loud 思路，后续做）

## 9-x 第九轮修复批 · 2026-08-05（第九轮实测，DONE N=6）

- **9-1** wait_file 路径锚定 + CWD 铁律（已单列）
- **9-4** wait_file 加 `--any`（任一目标就位即返回）——辩论"立论或终结谁先来"场景；第九轮纳特双文件等【反方立论+终结】卡 15 分钟（AND 语义不适配）
- **9-5** 公告牌指南补"任务描述不得覆盖玩法文件路径口径"——第九轮公告牌写产出目录 vs 玩法文件写任务目录 → 正方按公告牌/反方按玩法文件 → 路径分叉卡 12 分钟
- **9-6** 角色模板补"人工介入/对话 ≠ 下线"——回应窗口消息后必须继续 poll，对话不是收工轮（第九轮乔布斯被介入后习惯性下线）
- **9-7** _reasonix_poll.js TIMEOUT 输出加热提醒"⚠️ 未到收工轮前禁止结束回合"——人工指令比模板文字有效（用户对乔布斯说"禁止在一个会话回合内下线"后它乖乖轮询），机制化：每轮 poll 都看到
- **9-8** 角色模板补"消息来源识别"——不默认窗口消息是大鱼（内容含 大鱼_老渣对讲 路径=大鱼；monitor 视角/总指挥指令=用户）；身份不影响执行，收工轮才是下线唯一依据（乔布斯自述认账）

## 9-1 wait_file 路径锚定 + CWD 铁律 · 2026-08-05（第九轮乔布斯事故启示）

- **wait_file.js 路径自动锚定**：脚本用 __dirname 推导角色目录/项目根，参数以 `../` 开头（相对角色目录写法）自动转绝对路径——**与 CWD 无关**，cd 进 临时脚本 再跑也路径正确（已实测：CWD 偏移下 ../ 锚定正确退出码 0）
- **角色模板铁律 1 补 CWD 约束**：所有 `_*.js` 工具（_reasonix_poll/_deliver/_sign/_lock/_poll/wait_file）必须在角色目录 CWD 下跑，禁止 cd 进 临时脚本/ 子目录再调用；wait_file 调用示例补全路径 `node 临时脚本/wait_file.js`（不 cd）
- **起因**：第九轮乔布斯 poll 到活跃牌后进程挂死（回合中断），monitor 误诊死法——虽非 wait_file 的锅，但"cd 进临时脚本导致相对路径偏移"是真实隐患（本次未踩中，防御性加固）

## 8-3 自检轮 22 条全部落地 · 2026-08-05（第八轮 004 自检轮产出 → 角色模板）

- **表述不清 7 条**：①非活跃轮跳过的是干活+签字不是轮次推进 ②WOKEN 处理路径（_wakeup.md 改名_acked + 节奏普通3s/待命15s）③收工判定以退出码2为准 ④待命轮标准写法（半角冒号+空格`模式: 待命`）⑤read_bulletin 伪代码改真实命令 ⑥--loop N 用法定义 ⑦删除前先问问的对象=老渣
- **矛盾 8 条**：⑧流水账英文→默认中文 ⑨while 阻塞加场景限定（普通轮禁止/待命休眠轮必须）⑩铁律4/5 改判定条件式（单写者跳过锁/默认原生write_file）⑪process.cwd() vs ../我的世界 两套路径各自适用场景 ⑫（并入⑩）⑬heredoc 场景化（含转义用write_file/纯文本可heredoc）⑭铁律9 中文长内容统一原生write_file直写 ⑮一次性任务模式判定权=启动指令原文
- **约束加固 7 条**：⑯（并入②）⑰收工轮先确认退场文件再输出项目完成 ⑱续心跳量化（>2分钟无poll动作则每30s写）⑲求助探测动作+升级路径（3轮无回复重写）⑳依赖缺失兜底分支（复查路径→查搭档心跳→求助→先做不依赖部分）㉑待命20分钟超时后继续poll不写求助 ㉒临时脚本删除问老渣（并入⑦）
- 来源：004 自检轮三份 AGENTS 自检报告（纳特 9 条/乔布斯 11 条/图灵 9 条，全带原文引用+建议改法）

## 8-2 退场口径统一 + 铁律9边界 · 2026-08-05（第八轮图灵审计 + 大鱼自证）

- **角色模板 case 2 注释改口径**（图灵审计第 1 条）：删"写退场文件后保持 N 不变继续 poll 等追加"——改为"收工轮 → 退场文件 → 流水账 → 项目完成 → 停止循环（回合结束）"；追加轮由大鱼新会话发起，退场后无需 poll
- **_reasonix_poll.js 5-1 注释改写**（图灵审计第 2 条）：注明"本豁免仅服务同一回合内防 RETIRED 误跳号（断点续接/唤醒），不构成退场后继续轮询的依据"
- **退出口径一致性审计**（图灵审计第 3 条）：三处退出时机（唯一退出时机/唯一合法退出/唯一停循环时机）统一——收工轮退场文件创建 + 项目完成输出 = 回合终点，无例外延伸
- **大鱼模板铁律 9 加边界**（大鱼自证）："项目期间" = 开工到收口（DONE + 三件套），收口后铁律 9 失效合法结束回合；收尾点补"收口后禁止输出'我不下线'占位话术"（输出即回合闭合）
- **起因**：第八轮图灵"退场后继续 poll 等 007"（实际执行正确但模板残留矛盾未抓）；大鱼收口后口头声明"不下线"

## 8-1 收尾点 + 口头声明=下线 · 2026-08-05（第八轮实测暴露）

- **大鱼模板（窗口常驻+run拉起）回合保持铁律精化**：「我还在盯/我不下线/继续轮询」这类**口头声明 = 下线的瞬间**——输出最终回复回合即结束，你无法感知自己的回合边界，**保持在场唯一机制 = 回合内循环**（_reasonix_poll.js --loop / bash while 持续工具动作），禁止用口头表态代替循环；**收尾点**：monitor DONE + 收工三件套写入 = 项目收口 → 此时才输出最终回复
- **角色模板收工轮补**：输出「项目完成」= 合法收尾点（回合结束），**退场后不要再 poll 等下一张牌**（第八轮图灵退场后等不存在的 007）
- **起因**：第八轮大鱼收工后声称"不下线等下一轮"（实际声明即下线）；图灵退场后继续 poll 等追加——模板只有"防提前下线"没有"收口后正常结束"语义

## 7-4/7-5 角色体验报告建议落地 · 2026-08-04（第七轮角色体验报告 → 模板）

- **7-4 待命轮假唤醒修复（纳特卡点1）**：角色模板待命轮询代码块 case 1（被唤醒）从 `break` 改 `sleep 15` 继续循环——monitor 可能在角色启动前写过 _wakeup.md（迟到唤醒），WOKEN 后 break 反复打断待命循环；继续循环后下一轮 poll 自然检查公告牌/收工轮。_reasonix_poll.js 逻辑本就正确（只有未 ack _wakeup.md 才 WOKEN），未改
- **7-5 三件套（纳特/乔布斯建议）**：①语言规则统一——4 个玩法文件"内容用英文"→"默认中文，与搭档/公告牌一致"（第七轮实操全中文，指令歧义消除）②辩论引用纪律——引用公告牌/AGENTS.md/对方原文必须逐字准确，不得用括号改写原文（第七轮正方 T2 加括号被抓）③wait_file.js 沉淀——标准等文件脚本（0.5s 轮询+30s 续心跳+超时兜底+父目录检查），scaffold 分发到角色 临时脚本/ + 工具源码快照，模板等文件铁律区引用

## 7-3 大鱼自检 7 条落地 · 2026-08-04（第七轮大鱼自检报告 → 模板）

- **大鱼模板（窗口常驻+run拉起）按自检报告落地 7 条**：①"在场"=每回合拉 _fish_loop.log + 做决策（发布/打回/补搬/唤醒/等），非脚本在跑 ②启动命令必须 `> _fish_loop.log 2>&1` 重定向（日志 mtime=在跑硬证据）③唯一第一件事=先看牌，启动脚本在看牌后（消两处"第一件事"矛盾）④铁律0"闭嘴"精化=不闲聊不抢先，用户对话先干活再最短汇报 ⑤脚本在跑≠在场写死（7-1 精化）⑥DEAD 先复核 3 分钟内新产出再唤醒（monitor 已有判据，模板写明）⑦追加发布时机=批次放回完整即发布（信号消失是收口确认，防 1 分钟窗口卡死）
- 来源：大鱼自检报告 7 条（006 追加轮大鱼自检任务产出，全带原文引用+建议改法）

## 7-2 scaffold 部署 _fish_loop.js · 2026-08-04（第七轮实测暴露）

- **scaffold.js 补 _fish_loop.js 部署**：fish 模式部署清单（_wakeup.js 后）+ 工具源码快照清单——第七轮实测大鱼目录缺 _fish_loop.js（scaffold 漏部署），大鱼只能按规格自补临时版（49 行 vs 官方 108 行，功能不全）

## 7-1 大鱼回合保持铁律 · 2026-08-04（第七轮实测暴露）

- **大鱼模板（窗口常驻+run拉起）补 2 条**：①启动铁律第 4 条「你 ≠ _fish_loop.js」——脚本在跑只是工具运行≠你在场，校验/发布/补搬/追加判定/异常处理全要 AI 决策；②周期验证节「回合保持铁律」——禁止把"我盯着呢/继续轮询"当最终回复输出（无工具调用的最终回复=回合结束=静默下线），输出最终回复前自检后续有无需决策事件，有 → 用 _reasonix_poll.js --loop 保持回合
- **起因**：第七轮实测大鱼把 _fish_loop.js 在跑 = 我在盯 搞混，静默下线后项目无人决策（角色侧不受影响，但追加/补搬/异常处理悬空）

## 6-1/6-2 修复 · 2026-08-04（第六轮实测 + 纳特澄清，测试基线 a59f145）

- **6-1 续心跳认知修正（角色模板）**：待命段铁律明确「跑 `_reasonix_poll.js --standby` 每次 poll 自动写心跳 30s 间隔，心跳持续在续不会断；离线期（会话间隙/进程不在）心跳必然断属正常，monitor 唤醒记录积压到下次快路径 poll 处理」——**纳特澄清：805s 断档是会话间隙非待命裸等**，早前"待命模板缺续心跳"归因错误已修正
- **6-2 依赖路径与发布时刻一致（公告牌指南）**：依赖路径必须与发布时刻实际文件位置一致；"归档位置"与"依赖本体位置"分开标注（归档=概要，本体=产出/…）——纳特 N=002 曾因公告牌写归档前路径 find 去回收站找本体

## 实测记录 · 2026-08-04 SoulForge v2.5 升级实弹

- **测试基线**：codex-ninja 分支 `test-monitor-alert`，HEAD `6619fb6`（含 --loop N/警告栏/监控日志/挂死识别/流程体验报告/升级计划/_fish_loop 全部实施态）
- **角色模板版本**：同分支 scaffold 生成（沙箱收紧 我的世界 + 等文件内联轮询 + 铁律强化 + 心跳契约）
- **被测对象**：SoulForge 工作副本 `我的世界/soulforge`（真仓库 v2.4.0 → 实测产出 v2.5.0/2.5.1/2.5.2 已同步回真仓库）
- **测试内容**：三角色组队（林纳斯 vs DHH 辩论 + 图灵裁判）跑完整流程——001 辩论定案 → 002 Sprint 落地（31 模式迁移 + 执行器 + 零漂移回归）→ 003 收工；验证 Skill 全流程（公告牌/待命轮/追加/收工/心跳/锁）
- **覆盖的玩法模式**：✅ **辩论**（001：正方/反方/裁判三阶段 6 份材料）✅ **单人输出**（002：三人并行 + 任务间依赖链等文件）✅ **收工**（003：流水账→退场文件→退场）✅ **待命轮**（002 后全员待命等通知）✅ **追加轮**（002/003 由老渣侧按场上结果供给）；❌ **未测**：双人对话、主笔审核（留待后续）
- **实测暴露问题**：①大鱼 sleep 300 盲等新公告牌（→ 升级计划第 2 条 + _fish_loop 已实施）②收工轮格式规范（产出负责人/任务目录多余字段）③贾维斯越权发布教训 ④启动阶段角色行为未收集（角色已下线不可补）⑤SoulForge v2.4 残留 extractTsAst 语法错误（已修 v2.5.1）
- **结论**：流程闭环可跑通；"AI 自由发挥"是主要风险源（已用脚本化缓解）；角色/大鱼窗口关闭后信息不可补，观察类任务必须在窗口存活期内完成

---
           —— 初代 --reasonix-f 方案F 大鱼调度 ——

## 2026-08-03（收官复核残留 3 处补齐：F-2 审核侧 signal / F-1 dir quote / F-14 fish 文件消歧）
- **F-2 审核侧补全**：主笔审核审核方改名 `请审核.md → _已处理` 时，同名 `请审核.md.signal` 一并归档（`_signal_acked`）——主笔重发前空窗期不再旧 signal 秒返→读已改名 .md→ENOENT
- **F-1 dir 补 quote**：start-fish 的 `--dir fishDir` 同样手动双引号包裹——项目根路径含空格（如 `D:\My Projects\项目A`）时不被 cmd 拆分
- **F-14 fish 文件消歧**：`node scaffold.js 项目 fish`（无第4参）且 CWD 存在名为 fish 的 roles 文件时，按 roles 文件处理（init）而非误入 fish 模式——实测：MODE: init + 角色正常生成

## 2026-08-03（收官复查修复：26 项新发现 F-1~F-26）
- **运行/流程（F-1~F-9）**：F-1 start-fish shell:true 参数拆分（实测）——prompt 手动 quote 包裹防 cmd 拆分；F-2 主笔打回轮 .signal 随 .md 一起改名归档；F-3 run 形态主笔审核改"回合内循环"（打回不再断链）；F-4 辩手等裁判改为等 .ready（裁判只写 .ready 不写 .signal）；F-5 启动指南修正（删 toml 无效 → 删整个角色目录重跑）；F-6 双人对话问方按产出负责人判断才交付（防双写）；F-7 辩论 :86 统一改名而非删除；F-8 单人输出文档类 deliver 带任务目录参数；F-9 run 收工 prompt 顺序对齐（先退场文件后流水账）
- **代码边界（F-10~F-17）**：F-10 monitor 自检格式B readdirSync 包 try；F-11 格式A/B 判定改按尾斜杠（无扩展名产出不再误判目录，实测）；F-12 fallback 任务前缀加边界（任务001 不命中 任务0010）；F-13 _sign 轮次必须是 >=1 整数（0/0.5 不再静默 完成_000.md）；F-14 scaffold isFishMode 消歧（roles 文件叫 fish 不再误入）+ desc 缺失兜底 name；F-15 _lock.js + 模板内联 lock 等锁期间续心跳（防 2min 误判 DEAD）；F-16 _poll --wakeup/--phase 缺参报错；F-17 monitor 复检忙等 → Atomics.wait
- **文档一致性（F-18~F-26）**：指南常见错误5 标已修复、fallback 标注仅格式A；设计文档 10-15s/240s 标失效、最后更新同步；BUG清单间隔/PowerShell 耗时统一、占位符举例更新；团队须知工具位置修正（角色目录非我的世界）

## 2026-08-03（第五轮修复：复核确认 2 项）
- **monitor allRoles 状态限定**：主流程收工分支改用与自检一致的 `状态：退场/休眠` 限定正则——`- 备注（补充）: xxx` 等任意自定义字段行不再被当角色（黑名单只覆盖已知 6 字段，限定正则根治）
- **H-1 迁移提示**：修复前生成的角色目录玩法文件残留 `{{ROLE_NAME}}`（等文件内联循环心跳写错目录→误判 DEAD）。scaffold add 对已存在角色 SKIP 不覆盖，升级角色 = 删除该角色目录重跑 scaffold 生成——见 启动指南.md「前置配置」角色目录版本同步提示

## 2026-08-03（第四轮修复：第三轮审查遗漏项——代码组中危 10 项）
- **monitor 收工轮字段行黑名单**：retireRe 状态限定（只认 `状态：退场/休眠` 行）+ allRoles 加黑名单——`- 模式: 收工（...）`/`- 备注（补充）: xxx` 等带括号字段行不再被当角色（修复前收工轮卡死）
- **monitor 健壮性**：HELP 自动回复 writeFileSync/renameSync 包 try（Windows rename EPERM/双跑 ENOENT 不再 CRASH）；写唤醒文件包 try；格式B readdirSync 包 try；自检 readFileSync 包 try；state.N 正整数校验
- **死锁唤醒**：搭档正则补句号终止符；写 _wakeup.md 前重读搭档心跳（刚恢复则跳过，与主唤醒路径一致）
- **`· 收工` 行首锚定**：monitor 三处 + _reasonix_poll checkRetire——任务描述含"· 收工"不再误判收工/提前退场
- **scaffold replace 函数化**：4 处 `String.replace(regex, 字符串)` → 函数返回值——角色名/背景含 `$&`/`$'`/`$$` 不再被当替换模式注入

## 2026-08-03（第三轮深度审查修复：运行缺陷 4 项）
- **H-1 scaffold 玩法文件替换占位符**：生成角色时 `_双人/主笔/辩论模式.md` 的 `{{ROLE_NAME}}` 心跳路径改为真实角色名——此前生成物含字面占位符，等文件内联循环心跳写入错误目录导致误判 DEAD（影响所有双人/主笔/辩论轮次）
- **H-2 monitor 收工轮产出行不阻塞收工**：收工轮误写未就位产出行时强制 `outputReady=true`——此前全员已退场仍 WAIT 永不 DONE（与"只看退场文件"承诺矛盾）
- **H-3 monitor 产出正则行首锚定**：`(?:^|\n)- 产出[:：]` 锚定产出行——任务描述中的"产出: 我的世界/…"引用文本不再被当本轮产出行验证
- **H-4 monitor fallback 限定任务目录**：产出兜底扫描仅查 `产出/任务NNN_*`（当前轮）——避免跨轮同名 .ready 误命中导致提前 DONE

## 2026-08-03（全面审查修复：复核报告 §五 四批）
- **运行期行为修复**（第一批）：monitor.js 产出正则兼容全角冒号（H1）、待命轮输出 STANDBY 不再误报收工检查（H2）、自动回复不覆盖大鱼具体回复（H3）、收工轮正则兼容半角括号+黑名单兼容全角冒号+组前缀剥离（H8）、自检状态正则补 \s*（M1）、心跳检测排除已退场角色（M2）、流水账"全程待命"单行豁免（M16）、死锁搭档正则支持行尾（L12）；_reasonix_poll.js 基于 __dirname 解析路径+statSync 异常保护（M3）、时钟回拨心跳守卫（L16）、signal 检测标注兼容性（L4）；_deliver.js 慢路径多公告牌强警告（H9）
- **配置缺口修复**（第二批）：scaffold fish/init 均生成 火影-大鱼/reasonix.toml（H10）；_lock.js + 模板内联 lock 过期锁 pid 存活校验/并发防护/Atomics 休眠（M9/L13）；start-fish.js 崩溃自动重启×5（M8）；scaffold 输入校验（L15）
- **流程指引**（第三批）：双人对话答方 deliver 指引（H5）、辩论无裁判交付定义（H7）、run拉起待命轮/收工轮调度规则（H6）、max-steps 参考注（L11）、签字表述统一（M20）
- **一致性清理**（第四批）：辩论模式不写 _deadlock（H4）、文档翻篇/逐轮搬运旧表述清理、v2.x 版本号清理、心跳描述按运行形态区分、隐患清单/外部BUG清单更新、设计文档失效标注、spawn 术语统一、完整示例产出路径、Multi-pass 灵魂路径等（M10-M19/L1-L16 余项）

## 2026-08-03（reasonix-f：三点经验 + 废弃模板清理）
- **三点经验落 Reasonix版模板**（`Reasonix版_角色_AGENTS模板.md`）：①全员待命轮规范（最后一轮全员标待命等通知，角色才持续 poll；别把待命轮混中间否则角色自作主张下线）②待命=强制动作（必须真正发起 poll 循环/禁止纯话术最终回复，否则 turn 结束即静默下线）③收工轮后才合法退出
- **删废弃模板** `assets/角色_AGENTS模板.md`（Codex 历史版，scaffold 已用 Reasonix版；回收站保留本地版）
- **引用清理**：README/SKILL/隐患清单 指向 Reasonix版
- **休眠/待命间隔对齐**：待命=15s poll（--standby）、休眠=3s 高频 poll；心跳间隔（standby 30s/休眠 15s）与 poll 间隔区分说明
- **公告牌指南修正**：产出类型收工轮不需要、收工范例措辞、全员待命轮收口提示、等通知入枚举

## 2026-08-02（F 方案开发：大鱼调度，reasonix-f 分支）
- **F 方案（大鱼调度）设计+核心开发**：多角色按需唤醒、干完即退，解决 E 全并行内存峰值高 + D 冷启动慢
  - 大鱼模板加「F 模式调度」节：周期 monitor + 按 `ROLE DONE|PENDING` 唤醒活跃角色 + 收工审计
  - 角色模板加「F 模式：被大鱼调度（干完即退）」：每次唤醒只干一轮，不循环
  - monitor.js 新增 `ROLE <角色> DONE|PENDING` 输出（大鱼调度判断依据）
  - 新增 scripts/start-fish.js：一键启动大鱼调度者（唯一常驻进程）
  - 启动指南加「F 模式启动」节
  - **实测依据**：`--continue` 省 34%（14.2s→9.4s）、按目录隔离不串、会话 10 分钟+仍有效；文件记忆接力是主通道，会话记忆仅提速不依赖
  - 设计文档：阅览室/大鱼调度方案_F方案_设计文档.md

           —— 初代 --reasonix 方案E 守护体系 ——

## 2026-08-02（E 模式守护者 + 依赖保障）
- **大鱼转型守护者**（双模式定位：A 逐轮搬运 / E 全量发布+守护）：
  - 发布前公告牌校验 5 项（编号连续 / 收工轮格式 / 角色枚举 / 前置依赖 / 状态流转），不合格打回老渣，不替改
  - E 模式：全量发布一次到位 + 60s 周期 monitor 验证（只验证不干预推进）+ 求助具体回复（非模板话术）+ 掉线唤醒
  - 收工报告精简：砍 流程合规验证.md，产出总结.md 含**逐轮完成矩阵**（公告牌要求 vs 实际签字/产出/退场，缺失标红——验证"谁跳过"的硬证据）
- **前置依赖保障**：公告牌完全指南加"任务依赖必须显式写依赖路径"规则 + 角色模板加"等前置文件就位"铁律（sleep 2 + existsSync，20 分钟超时兜底）
- **README 同步**：快速开始加启动方式二选一（E 全自动 / A 手动）、核心机制加 E 模式守护条目

           —— 初代 --reasonix 方案E 复查一致性 ——

## 2026-08-02（E 版本整体复查）
- **E 版本整体复查修复 9 项**（独立复核 + 修复）：
  - README 版本定位更新：方案A+E、永动能力改为"回合内等效永动（实测）"
  - 角色模板：turn 内循环收工步骤补对讲目录路径；「怎么干活/待命/休眠」标注仅限逐 turn 模式；「向大鱼求助」补 E 模式单次 poll 提示
  - `_reasonix_poll.js` 收工正则兼容 `· 收工` 格式（与 monitor.js 对齐）
  - 启动指南：「启动流程」标注 A 方案、方案甲/乙与方案A/E 命名澄清、旧项目 [sandbox] 迁移说明
  - SKILL.md 三种运行模式补 turn 内循环（E，全自动首选）+ 推荐表更新

           —— 初代 --reasonix 方案E 实测修复 ——

## 2026-08-02（E 方案多角色实测 + 修复）
- **方案E 多角色全流程实测通过**：4 角色（双人对话 11 轮 + 主笔审核打回→通过 + 收工轮），公告牌全量发布推进正常，产出/签字/退场齐全，成本约 ¥0.35-0.4（详见阅览室升级方案 §四.7）
- **实测修复 3 个问题**：
  - `_reasonix_poll.js` 快路径补公告牌检查：全量发布后目录 mtime 不再变化，快路径跳过公告牌检测导致永久 TIMEOUT（问方/答方/审核全踩）——快路径补 公告牌_{N+1} 单文件 existsSync
  - scaffold 角色 reasonix.toml 补 `[sandbox] workspace_root`：角色 write_file 不再被全局沙箱拦截，免 bash 绕行
  - 角色模板收工步骤明确退场文件路径（对讲目录下）：实测主笔把退场文件写到 我的世界/ 根，monitor 只在对讲目录验证

           —— 初代 --reasonix 文档清理 ——

## 2026-08-02（Codex残留清理）
- **文档全面清理 Codex 残留**：15个文件，去 REPL/240s/ninja init 等平台专属措辞
  - README/SKILL/团队须知/工具速查/公告牌指南：REPL→Node，240s→bash while
  - 大鱼模板/Reasonix版角色模板：去 REPL 监控轮询引用
  - 4个玩法文件：本轮后改为参照 AGENTS.md bash while，不再引用 _轮询片段.md
  - 设计文档：补 Reasonix 阅览室索引
  - 新增 启动指南.md（方案A）
  - 修复 --sleep 参数不存在、待命缺 --standby
  - 删除 _轮询片段.md、scaffold 不再复制
  - _reasonix_poll.js 逻辑未动


---
           ——  初代 --reasonix 单次探测优化 ——

## 2026-08-01（单次探测优化）
- **心跳批量 + mtime 预检 + 消除冗余 statSync + --standby 参数**
  - _reasonix_poll.js：心跳写入改为批量（_hb_state.json 记录间隔），休眠 15s / 待命 30s，减少写入量 50-80%
  - _reasonix_poll.js：新增 mtime 预检（_mtime.txt 缓存目录 mtime），无变化走快路径跳过全量文件检查；快路径保留唤醒检查（_wakeup.md 在子目录不影响 worldDir mtime）
  - _reasonix_poll.js：信号检测 existsSync → try-catch statSync，复用 stat 结果省去重复 mtime 获取
  - _reasonix_poll.js：新增 --standby 参数，区分休眠/待命心跳间隔（bash 模板暂不接入，统一 15s）
  - 单次探测架构不变，不改 bash 模板
           
           —— 初代 --reasonix 单次探测 ——

## 2026-08-01（单次探测）
- **移除内部循环，改为单次探测**
  - _reasonix_poll.js：砍掉 while(true) + safeSleep，改为单次扫描毫秒退出，循环和休眠上移到 Agent bash while
  - Reasonix版_角色_AGENTS模板.md：--max-wait 240 → 外置 sleep 15/3，bash case 3 分支由空转变 sleep
  - 终端不再长时间阻塞，单次调用毫秒级结束，遏制上下文膨胀

           —— 初代 --reasonix 补充 ——

## 2026-08-01（崩溃修复）
- **忙等崩溃修复 + --forever 模板同步**
  - _reasonix_poll.js：纯忙等 → safeSleep()（Atomics.wait 真休眠），降级 100ms 短切片兜底
  - Reasonix版_角色_AGENTS模板.md：bash 示例默认加 --forever + TIMEOUT 分支，一次 node 进程跑到命中

           —— 初代 --reasonix ——

## 2026-08-01（Reasonix 适配）
- **Reasonix 分支适配**：基于 Codex 版 v1.0.5，创建 reasonix 分支
  - _reasonix_poll.js 新增 --forever 参数：永久轮询，不限超时，直到公告牌/唤醒/收工才退出（Reasonix 无 REPL 300s 硬上限，天然支持无限等待）
  - 新增 _reasonix_poll.js：独立 Node 轮询脚本，bash 调用代替 REPL js()
  - 新增 Reasonix版_角色_AGENTS模板.md：bash while + node 替代 REPL 内联 js()
  - scaffold.js 适配：模板源切换 + _reasonix_poll.js 加入复制列表
  - 不动已有文件，协议不变，Codex 版和 Reasonix 版共存


           —— 初代 --v1.0.5 ——

## 2026-08-01（第三轮）
- **持久化轮询日志**
  - 搭档等文件超时日志：双人对话/主笔审核/辩论的等文件代码中（共 3 处），写 _deadlock 时同步追加轮询日志（含 mkdirSync 兜底）
  - 轮询转折点日志：_轮询片段.md 注入 _log() 函数，6 个转折点全埋点（mkDirSync 兜底 + 项目边界分隔线）
  - 影响范围：_轮询片段.md、_双人对话模式.md、_主笔审核模式.md、_辩论模式.md

           —— 初代 --v1.0.4 ——

## 2026-08-01（第二轮）
- **测试反馈修复 + 终审打磨**
  - 主笔审核 deliver 时机修正：deliver() 从步骤1移到审核通过后，.ready = 审核已通过，monitor 不再提前翻篇
  - wakeup 死循环修复：_轮询片段.md 两处 + _poll.js 一处 unlinkSync → renameSync，改名代替删除消除竞态
  - 辩论模式修正：步骤 2/4 移除等自己指令、.signal 文字指引外提、裁判区双模说明精简、YFPP 去重
  - {{ROLE_NAME}} 模板修复：角色模板 L1 恢复 # {{ROLE_NAME}}，身份声明降为 H2
  - 铁律/参数/措辞同步：铁律 #3 改名标记、大鱼标注原 20-30s、设计文档 13s→15s

           —— 初代 --v1.0.3 ——

## 2026-08-01
- **通信效率优化**：信号文件通知 + 轮询代码去重 + 循环内优化 + monitor 自适应复检
  - 轮询代码去重：创建 _轮询片段.md 为唯一权威来源，角色模板 + 4 模式文件全部引用，消除约 450 行重复代码
  - 公告牌就绪信号：大鱼写 _round_NNN.signal，角色 1s 内检测到翻篇，端到端检测延迟约 7s→1s
  - 搭档间信号文件通知：搭档写 .signal 文件，对方 0.5s 轮询信号而非完整文件，平均等文件延迟约 0.75s→0.25s
  - 去重复 import：_轮询片段.md + 等文件模板全部 import 提到循环外
  - 心跳批量写入：休眠每 5 周期(15s)、待命每 2 周期(30s)写一次心跳，写入量减少 75-80%
  - monitor 快速复检：产出目录 mtime 近期变化触发 2s 快速复检（最多 5 次），触发条件为 mtime 而非签字
  - 影响范围：_轮询片段.md（新增）、角色模板、大鱼模板、4 模式文件、monitor.js、_poll.js、团队须知、设计文档

           —— 初代 --v1.0.2 ——

## 2026-07-31
- **通信效率优化 v2.19**：五大优化降低项目端到端延迟
  - REPL 轮询 O(1) 化：_lastN 跟踪机制取代 readdirSync 全量扫描，每次轮询省掉目录遍历
  - 目录 mtime 预检移植到 REPL：公告牌目录无变化时跳过文件检查（Shell _poll.js 已有，现 REPL 也覆盖）
  - 轮询间隔全面降低：待命 60s→15s、休眠 5s→3s、搭档等文件 3s→1.5s、monitor 活跃轮 20-30s→10-15s
  - monitor.js 快速路径标注：产出检查在签字之前完成，产出未就位直接跳过签字/心跳/死锁扫描
  - _poll.js Shell 版同步：渐进间隔提速 + 低功耗 60s→3s
  - 影响范围：角色_AGENTS模板.md、4 个模式文件、大鱼_AGENTS模板.md、monitor.js、_poll.js、设计文档

           —— 初代 --v1.0.1 ——

## 2026-07-30
- REPL 自愈窗口从 280s 提前到 240s：300s 硬上限前留 60s 安全边际，避免自退输出被 REPL 硬杀截胡，消除 280-300s 轮询盲区（5 代码文件 + 3 份文档同步更新）
- 角色模板待命+休眠轮询加调试日志（时间戳 + 当前N值）
- CHANGELOG 重写为时间线格式

           —— 初代 --v1.0.0 ——

## 2026-07-30
- deliver() 统一简化：Shell版砍掉文件拷贝逻辑，与REPL版行为对齐——只写 .ready 信号，不搬运文件
- deliver() 新增 sourcePath 可选参数：代码模式写入源文件路径到 .ready，方便追溯（REPL + Shell 双版同步）
- 四个模式文件加代码/文档双模式 deliver 指引：代码类源文件原地改完直接发信号，文档类先写内容再发信号
- REPL deliver 原子写入对齐：.tmp → rename，与 Shell 版一致
- 公告牌完全指南 Section 六重写：精确描述 Monitor 两种产出检查格式（格式A逐项查 .ready，格式B目录扫 .ready）
- 公告牌完全指南 Section 六嵌入「代码 vs 文档」对照表：老渣写产出路径前先判断类型
- 公告牌模板（Section 二）加产出路径强制说明：必须指向 我的世界/产出/，禁止指向源文件目录
- Monitor v2.18 产出路径 fallback：格式A检查失败后自动扫描 产出/ 子目录兜底找 .ready，路径写错不卡翻篇
- 公告牌完全指南 Section 六补产出路径错位警告：指向源文件目录 = monitor 找不到 .ready = 产出检查形同虚设



           —— 初代 ——

## 2026-07-30
- 首个可永久运行版本

### 核心能力
- 多Agent协作：老渣写公告牌 → 大鱼搬运+监控 → 角色轮询干活，三方通过文件系统通信
- 四种协作模式：双人对话、主笔审核、辩论（正反+裁判）、单人输出
- 角色状态机：待命 → 活跃 → 休眠 → 退场，公告牌驱动流转
- 280s REPL自愈：轮询循环主动退出+AI自然重发，不依赖外部续命
- 双向心跳检测：角色写 `_heartbeat.txt` 保活，monitor 2分钟无心跳自动唤醒
- 死锁并行恢复：等文件超时写 `_deadlock.md` + 自己继续推进，双路径恢复
- 裁判超时兜底：裁判20分钟等不到总结 → 读已有材料给结论
- 三层容错：心跳自愈 + 死锁并行 + 产出兜底（签字降级不阻塞翻篇）
- 原子文件操作：`.tmp → rename`，搭档不会读到半截文件

### 配套文档
- `公告牌完全指南.md`：格式、状态机、多轮规划、容错机制、常见错误
- 桌面 `codex-ninja.md`：设计理念、三方协作、模式流程图、机制详解、审核清单
- `团队须知/AGENTS.md`：注入所有角色窗口的通用规则

### 工具链
- `monitor.js`：翻篇监控 + 心跳检测 + 死锁检测 + 产出校验
- `_poll.js`：通用轮询工具（Shell备用）
- `_sign.js`、`_lock.js`、`_deliver.js`、`_wakeup.js`：签字/锁/交付/唤醒

           
