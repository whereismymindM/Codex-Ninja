# 启动指南

> 版本：reasonix-f（窗口常驻 / run 拉起 两种形态）
> 基于 **Reasonix v1.17.6-rc.1** 实测验证

---

## 两种运行形态（选一）

| | 窗口常驻 | run 拉起 |
|---|---|---|
| 角色怎么跑 | 角色窗口 `reasonix code` 常驻，自己 poll 公告牌推进 | 大鱼按需 `reasonix run --continue` 拉起角色，干完即退 |
| 大鱼模板 | `大鱼_AGENTS模板_窗口常驻.md` | `大鱼_AGENTS模板_run拉起.md` |
| 生成大鱼 | `node scaffold.js <项目目录> fish window` | `node scaffold.js <项目目录> fish run` |
| 适用 | **协作复杂**（辩论/主笔审核/多角色接力）、角色数 ≤6、需稳定推进 | **简单/串行**（单人输出/顺序轮次）、角色数多（内存压力大）、资源紧张 |
| 默认 | ✅ 推荐 | |

**选择依据**：协作复杂 → 窗口常驻；角色多/资源紧 → run 拉起；不确定 → 窗口常驻。

> 共享底座（公告牌格式、monitor、待命轮、扣留收工轮）两形态通用，见 `assets/老渣文档/公告牌完全指南.md`。
> ⚠️ 但 monitor 的心跳处理因 `_运行形态.mode` 而异：窗口常驻 = 2 分钟无心跳自动写 `_wakeup.md` 唤醒；run 拉起 = 角色干完即退心跳停是正常态，10 分钟才判且不自动唤醒（唤醒由大鱼调度负责）。

---

## 前置配置（两种形态都需要）

角色目录的 `reasonix.toml` 由 scaffold 自动生成（含 `[tools] bash_timeout_seconds = 0` 和 `[sandbox] workspace_root`）；手动项目加：

```toml
[tools]
bash_timeout_seconds = 0   # 关闭 bash 前台上限，角色可在回合内持续轮询直到收工
```

> ⚠️ 旧项目（scaffold 早于 2026-08-02 生成的）角色 `reasonix.toml` 可能缺 `[sandbox]`——**删掉该角色目录后重跑 `node scaffold.js <项目目录> <roles.json> add`** 会重新生成全套（单独删 reasonix.toml 无效：scaffold add 对已有角色目录 SKIP，见下条）；或手动补 toml。
>
> ⚠️ **大鱼 `reasonix.toml` 同样存在旧版问题**：scaffold 对 `火影-大鱼/reasonix.toml` 也是"不存在才写"（fish/init 两条路径均如此）——
> **2026-08-05 前生成的大鱼 toml 缺 `allow_write = [火影-大鱼]`**（大鱼写自己目录的 `_heartbeat.txt`/日志需此权限，缺则 write_file 被沙箱拦截 → bash 绕行 或 心跳写不进 → FISH_DEAD 误报）。
> **重跑 fish 重建不会更新已有 toml**。修复：删除 `火影-大鱼/reasonix.toml` → 重跑 `node scaffold.js <项目目录> fish window|run` 重新生成（toml 只有 bash_timeout/sandbox/allow_write 三条配置，无自定义内容，可安全删除重建）。

> ⚠️ **角色目录版本同步**：模板/工具脚本升级后，**已生成的角色目录仍是旧版**——scaffold add 对已存在角色 SKIP（不覆盖），升级角色 = **删除该角色目录 → 重跑 `node scaffold.js <项目目录> <roles.json> add`**。
> 典型场景：修复前生成的角色，`_双人/主笔/辩论模式.md` 里的 `{{ROLE_NAME}}` 心跳路径未替换，等文件内联循环的心跳会写入字面占位符目录 → monitor 读不到 → 误判 DEAD。
> ⚠️ **手动同步单文件模板必替换占位符**：不想删目录重建、只手动 cp 模板到角色目录时——**cp 是原样复制，scaffold 才会替换 `{{ROLE_NAME}}`**；不替换 = 心跳路径写进字面 `{{ROLE_NAME}}_大鱼对讲/` → monitor 误判 DEAD。
> 手动同步后必做：①`sed -i 's|{{ROLE_NAME}}|<角色名>|g' <文件>` ②`grep -c '{{ROLE_' <文件>` 验证 0 残留。**推荐路径仍是删目录重建**（scaffold 自动替换），手动 cp 仅限单文件小补丁。

### 角色原生工具权限（reasonix.toml 必配，别省）

角色要有**原生 write_file 权限**直接写 我的世界/（共享空间），必须配好 `[sandbox]`：

```toml
[sandbox]
workspace_root = "D:/Codex/workspace/口袋忍者/大鱼号/一号舱室-软件开发部/我的世界"   # write_file 沙箱根=我的世界（角色只写干活区，绝对路径 / 分隔；read_file 读公告牌/角色目录不受限）
```

- **作用**：`workspace_root` = write_file 沙箱根 = **`我的世界/`**（scaffold 自动生成，已收紧）→ 角色用**原生 write_file 工具**直接写共享区，免 bash 绕行（`cat > file` 之类）；`read_file` 全局可读不受限（角色仍能读 AGENTS.md/玩法文件/公告牌）
- **不配的后果**：write_file 被沙箱拦截 → 角色被迫走 bash 绕行 → **慢 + 易错**（实测踩过：角色写文件全走 bash，轮询被拖垮）
- **配错路径的后果**：workspace_root 不是 我的世界 → 角色写 我的世界/ 仍被拦，同样走 bash 绕行
- **排查**：角色动作明显变慢 / 日志里大量 bash 写文件 = 大概率 sandbox 没配好，检查该角色 reasonix.toml
- ⚠️ **沙箱已收紧**：角色/大鱼的 workspace_root = `项目根/我的世界`——write_file 只能写干活区，杜绝误写 monitor.js/角色目录；**现有角色需删除重建才生效**

**公告牌发布**：老渣把所有公告牌（001 → 收工）写完放大鱼目录（`火影-大鱼/`），大鱼发布时校验 + 全量复制到 `我的世界/`（有待命轮则扣留收工轮，10 分钟后补搬——见大鱼模板）。

---

## 形态一：窗口常驻（推荐）

### 启动大鱼

```powershell
# 进入火影-大鱼 目录，运行
reasonix code
# 输入「进入角色」——大鱼开始：发布校验 → 全量发布（扣留收工轮按需）→ 周期 monitor
```

### 启动角色（每个角色一个窗口）

```powershell
# 进入角色目录，运行
reasonix code
# 输入「进入角色」——角色开始 poll 公告牌，自己推进轮次
```

批量启动（每个角色目录并行开一个窗口）：

> 现成脚本：把 `scripts/start-all.ps1`（Skill 仓库内）复制到项目根目录，双击运行即可。
> 脚本自动识别角色目录（含 AGENTS.md + reasonix.toml），不再需要手动维护 skip 黑名单；防重复开窗；如需把火影-大鱼一起开：`powershell -File start-all.ps1 -IncludeFish`。
> 在每个窗口输入「进入角色」即可。

```powershell
# 复制即用（老渣操作）：
Copy-Item <skill路径>/scripts/start-all.ps1 <项目根目录>/
# 然后双击 start-all.ps1（或 右键 → 用 PowerShell 运行）
```

> ⚠️ 大鱼窗口（火影-大鱼/）默认不在此脚本内——按上方「启动大鱼」单独开；或用 `-IncludeFish` 一起开。

### 角色轮询机制

角色窗口常驻后，`bash while + _reasonix_poll.js` 自动循环：每轮 poll 公告牌 → 有活就干（读玩法文件 → 产出 → deliver → sign）→ 干完 N+1 → 继续 poll。非活跃轮（待命/休眠）自动跳过。**全程无人值守**，直到收工轮创建退场文件退出。

---

## 形态二：run 拉起

### 启动大鱼

```bash
node <skill>/scripts/start-fish.js <项目根目录>
# 例：cd 一号舱室-软件开发部 && node <skill>/scripts/start-fish.js
```

前置：
1. **scaffold fish run**（生成 run拉起大鱼 + `_运行形态.mode` 标志，monitor 靠它区分心跳处理）：`node <skill>/scripts/scaffold.js <项目根目录> fish run`
2. **公告牌已放大鱼目录**（老渣写好 001→收工）——启动大鱼后，大鱼按启动流程先看牌 → 校验 → 全量发布到 我的世界/（与窗口常驻同一套流程）；**发布完成前角色不会有反应，属正常**
之后大鱼自动开始调度循环——每 60s 跑 monitor，按 `ROLE <角色> DONE|PENDING` 输出判断该唤醒谁，用 `reasonix run --continue`（省 34% 冷启动）或冷启动唤醒，角色干完即退。

### 角色生命周期

角色初始不启动（或建会话后退出）→ 大鱼判断第 N 轮需要谁 → `reasonix run --continue/--dir` 唤醒 → 角色读公告牌 + 前置产出 → 干活 → deliver + sign → 输出最终回复退出（内存释放）→ 大鱼周期 monitor 检测完成 → 下一轮再唤醒。

**关键认知（文件记忆接力）**：每次唤醒 prompt 要求角色读公告牌 + 前置产出恢复上下文——不依赖会话记忆，冷启动也能正确接上；`--continue` 只是提速。会话过期只慢不挂。

---

## "永久轮询"的实现机制与边界

**"永久轮询"不是字面无限循环**——通过配置（`bash_timeout_seconds = 0`，scaffold 自动生成）+ 回合保持协议（角色在 poll 到收工轮前不输出最终回复）实现回合内等效永动（实测单回合 20+ 分钟），全程无人值守。

**终止条件**：收工轮创建退场文件 → 回合结束（这是唯一正常退出）。

**边界（撞墙降级，不丢进度）**：`max-steps` 是硬墙。耗尽后 turn 被强制结束，但文件系统进度（产出/签字/对话/退场）全部保留——重新 run 一次，角色按模板「启动自检」（签字存在→N++ 跳过已完成轮）从断点续接。

**`--max-steps` 参考值**（实测：3 轮项目约 40-60 步）：

> ⚠️ **口径区分**：窗口常驻形态 = 单会话跑完整个项目（含全部轮次），按下表配置；run 拉起形态 = 每次唤醒只干一轮，单轮唤醒用 `--max-steps 120`（见 run拉起模板），双人对话/辩论等"回合内循环"单轮按复杂度上调至 200+。

| 窗口常驻项目规模 | 建议 max-steps |
|---------|:--:|
| ≤3 轮 | 200 |
| 4-10 轮 | 500 |
| >10 轮 | 1000+ |

> run 模式**没有** `--gui` 参数（旧文档提过，当前 CLI 已无此参数）。要看过程就用 `-show-thinking`；要记账就用 `-metrics`。
