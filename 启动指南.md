# 启动指南

> 版本：初代 --reasonix 方案A / 方案E（turn 内循环）
> 基于 **Reasonix v1.17.6-rc.1** 实测验证（2026-08-02，deepseek-v4-flash/pro）

---

## 两种启动模式（code vs run）

| 维度 | `reasonix code` | `reasonix run` |
|------|-----------------|----------------|
| 初始指令 | 用户手动输入「进入角色」 | 老渣写的初始指令（命令最后一个参数，自动传入，无需手动输入） |
| 进程 | 常驻会话 | 一次性进程，干完退出 |
| turn | 多回合（常驻对话） | 单回合（干完即终）——**对 E 模式无影响**：E 要的正是单回合内跑完全部轮次 |
| 界面 | 交互式 TUI | CLI 流式日志（**无 GUI 窗口**）；`-show-thinking` 展开思考全文、`-metrics <路径>` 输出 token/成本 JSON |
| 适合 | 调试、人工盯场 | 自动化、E 模式全自动 |

> ⚠️ run 模式**没有** `--gui` 参数（旧文档提过，当前 CLI 已无此参数）。要看过程就用 `-show-thinking`；要记账就用 `-metrics`。

## 启动流程（A 方案·手动窗口）

1. 开角色窗口：进入角色目录，运行 `reasonix code`
2. 输入「进入角色」
3. 角色开始 `bash while + _reasonix_poll.js` 轮询公告牌
4. 每轮干完活进入休眠，bash while + _reasonix_poll.js 自动继续轮询下一轮（无需人工输入）

---

## 全自动前置配置（turn 内循环模式）

角色目录的 `reasonix.toml` 已由 scaffold 自动生成（含 `[tools] bash_timeout_seconds = 0` 和 `[sandbox] workspace_root`）；手动项目加：

```toml
[tools]
bash_timeout_seconds = 0   # 关闭 bash 前台上限，角色可在第一回合内持续轮询直到收工
```

**公告牌发布方式（方案甲·全量发布）**：E 模式不需要大鱼逐轮搬运和 monitor 翻篇——老渣把所有公告牌（001 → 收工）写完放大鱼目录后，一次性复制到 `我的世界/`：

```bash
cp 火影-大鱼/公告牌_*.md 我的世界/
```

> ⚠️ 旧项目（scaffold 早于 2026-08-02 生成的）角色 `reasonix.toml` 可能缺 `[sandbox]`——删掉角色目录的 reasonix.toml 后重跑 `node scaffold.js <项目目录> <roles.json> add` 会重新生成；或手动补上（见启动命令下方说明）。

角色回合内会从 001 顺序处理到收工轮（非活跃轮自动跳过），全程无人值守。若仍需逐轮搬运 + monitor 容错（方案乙），保持大鱼窗口运行即可，两种方式兼容。

> 📌 命名澄清：**方案A/E** 是运行模式维度，**方案甲/乙** 是公告牌发布方式维度——两套命名互不冲突。E 模式默认方案甲（全量发布），A 模式默认方案乙（逐轮+monitor）。

---

## E 模式启动命令（turn 内循环）

```bash
reasonix run --dir <角色目录> --model <模型> --max-steps 500 "读取 AGENTS.md，按「工作模式」节开始 turn 内循环。禁止输出最终回复，直到收工轮完成。"
```

批量启动（每个角色目录并行开一个）：

```powershell
# start-all-E.ps1 —— E 模式一键启动所有角色（自动进入回合内循环）
$skip = @("我的世界", "火影-大鱼", "档案馆", "产出")
Get-ChildItem $PSScriptRoot -Directory | Where-Object { $_.Name -notin $skip } | ForEach-Object {
    wt --title $_.Name powershell -NoExit -Command "cd '$($_.FullName)'; reasonix run --dir . --model deepseek-v4-flash --max-steps 500 '读取 AGENTS.md，按「工作模式」节开始 turn 内循环。禁止输出最终回复，直到收工轮完成。'"
}
```

---

## "永久轮询"的实现机制与边界（E 模式核心认知）

**"永久轮询"不是字面无限循环**，是三层机制叠加的近似：

| 层 | 机制 | 作用 |
|---|------|------|
| 1 | `bash_timeout_seconds = 0` | bash 等待无限——"等"永久 |
| 2 | 模型回合内不输出最终回复 | turn 不结束——"回合"持续（已实测单回合 20+ 分钟） |
| 3 | `--max-steps` 给足 | 步数墙抬到项目所需之上——"跑"等效永久 |

**终止条件**：收工轮创建退场文件 → 回合结束（这是唯一正常退出）。

**边界（撞墙降级，不丢进度）**：`max-steps` 是硬墙。耗尽后 turn 被强制结束，但文件系统进度（产出/签字/对话/退场）全部保留——重新 run 一次，角色按模板「启动自检」（签字存在→N++ 跳过已完成轮）从断点续接。

**`--max-steps` 参考值**（实测：3 轮项目约 40-60 步）：

| 项目规模 | 建议 max-steps |
|---------|:--:|
| ≤3 轮 | 200 |
| 4-10 轮 | 500 |
| >10 轮 | 1000+ |

## 批量启动（A 方案·手动窗口）

```powershell
# 排除非角色目录（填入你需要跳过的目录名）
$skip = @()
Get-ChildItem $PSScriptRoot -Directory | Where-Object { $_.Name -notin $skip } | ForEach-Object {
    wt --title $_.Name powershell -NoExit -Command "cd '$($_.FullName)'; reasonix code --model deepseek-v4-pro"
}
```

保存为 `start-all.ps1` 放到项目根目录，双击运行。在每个窗口输入「进入角色」即可。
