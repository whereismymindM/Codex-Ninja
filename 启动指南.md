# 启动指南

> 版本：初代 --reasonix 方案A

---

## 启动流程

1. 开角色窗口：进入角色目录，运行 `reasonix code`
2. 输入「进入角色」
3. 角色开始 `bash while + _reasonix_poll.js` 轮询公告牌
4. 每轮干完活进入休眠，bash while + _reasonix_poll.js 自动继续轮询下一轮（无需人工输入）

---

## 全自动前置配置（turn 内循环模式）

角色目录的 `reasonix.toml` 已由 scaffold 自动生成（含 `[tools] bash_timeout_seconds = 0`）；手动项目加：

```toml
[tools]
bash_timeout_seconds = 0   # 关闭 bash 前台上限，角色可在第一回合内持续轮询直到收工
```

**公告牌发布方式（方案甲·全量发布）**：E 模式不需要大鱼逐轮搬运和 monitor 翻篇——老渣把所有公告牌（001 → 收工）写完放大鱼目录后，一次性复制到 `我的世界/`：

```bash
cp 火影-大鱼/公告牌_*.md 我的世界/
```

角色回合内会从 001 顺序处理到收工轮（非活跃轮自动跳过），全程无人值守。若仍需逐轮搬运 + monitor 容错（方案乙），保持大鱼窗口运行即可，两种方式兼容。

## 批量启动

```powershell
# 排除非角色目录（填入你需要跳过的目录名）
$skip = @()
Get-ChildItem $PSScriptRoot -Directory | Where-Object { $_.Name -notin $skip } | ForEach-Object {
    wt --title $_.Name powershell -NoExit -Command "cd '$($_.FullName)'; reasonix code --model deepseek-v4-pro"
}
```

保存为 `start-all.ps1` 放到项目根目录，双击运行。在每个窗口输入「进入角色」即可。
