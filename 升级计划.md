# codex-ninja 升级计划（待办池）

> 记录已提出但尚未实施的功能/机制升级点。**讨论定案后才实施**；实施后移入 CHANGELOG。

---

## 2. 大鱼周期验证分层（2026-08-04 实弹暴露，待实施）

**问题**：大鱼用 `sleep 300` 做周期验证——sleep 期间不检测"新公告牌是否来了"，新牌最多等 5 分钟才发现（实测：002 放好 4 分钟+ 才被大鱼发现，期间项目停滞）。

**定性**：非纯盲等（醒来跑 monitor 有周期验证职责），但"新公告牌响应"是盲等成分——不知道何时来，睡死 5 分钟。

**方案（分层轮询，消除盲等）**：
- 每 30-60s：**轻量检查源目录公告牌 mtime**（stat，几乎零成本）→ 有新牌 → 立即校验+发布（秒级响应）
- 每 5 分钟：monitor 深检查（心跳/产出/签字）——频率不变
- 与 `--loop N`（poll 进程内循环）同思路：**长 sleep → 短周期轻检查**

**状态**：待实施（讨论定案）。

---

## 1. 全局公告牌写手（2026-08-04 用户提出，待讨论）

**需求**：不靠老渣逐轮写公告牌——由一个**常驻"公告牌写手"角色**根据场上结果自动写下一张公告牌，放在待命轮后面，让项目自动续轮（老渣只定总体目标，撒手）。

**关键机制（用户 2026-08-04 澄清，两轮）**：
- **写手也进任务轮**——它在公告牌角色列表里有位置，但没有具体干活任务（职责 = 监控 + 写牌）
- **写手一路推进到待命轮**——持续监控其他角色签字/产出，判定"上一轮完成 + 下一轮不存在"→ 写下一张牌（内容基于场上产出，如裁判结论），一路写到待命轮/收工轮
- 写手"待命"是常态（无干活任务），**主动观察**而非等通知

**发布机制（关键配套，用户指出）**：
- 现在公告牌是**全量发布**（大鱼一次性搬全部，角色一路 poll 到底）
- **之前是逐轮发布**（角色干完一轮、等大鱼发布下一张才推进）
- 写手模式天然配**逐轮发布**：写手写一张 → 大鱼发布一张 → 角色推进下一轮 → 写手继续监控——任务才能随场上变化真正动态调整（而非一开始全量铺开）

**示例场景**（SoulForge 升级实测）：001 辩论 → 裁判结论一出（图灵签字）→ 写手监控到 → 写 002（Sprint 落地，内容基于裁判结论）→ 大鱼逐轮发布 002 → 角色干完 → 写手写 003 收工轮 → 发布 → 收工。

**其余关键设计点（未定案，需讨论）**：
1. **写牌位置**：写手目录？还是直接写 `火影-大鱼/`？——写手只写牌不发布（发布/校验仍是大鱼职责，避免越权）
2. **完成判据（收口）**：老渣在 001 写"目标完成条件"（如"漏洞模式库回流 MVP 闭环即收工"）→ 写手据此判断何时写收工轮，防止无限续轮
3. **防死循环**：完成判据缺失时写手只写 N 轮/或等老渣确认；待命超时补收工机制（现有）兜底
4. **角色模板**：新增"公告牌写手"角色类型（scaffold 支持？）——本质是"只读场上状态 + 写公告牌"的轻角色，不参与干活

**状态**：待讨论 → 讨论定案后进入实施。

---

## 记录格式

每条升级点：**提出日期/提出人 → 需求描述 → 关键设计点 → 状态（待讨论/定案/实施中/已完成）**。完成项移入 CHANGELOG 并从本文件移除。
