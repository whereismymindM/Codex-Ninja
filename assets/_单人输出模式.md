# 单人输出模式

没有搭档，你一个人挑大梁。公告牌告诉你做什么、产出放哪。

---

## 怎么做

读公告牌 → 理解任务 → 动手干 → **用 REPL `deliver()` 交付**（零Shell依赖，0.1秒）→ 在对讲目录下写完成文件签字。

简单直接。别因为没人审就敷衍——自己是自己的第一道关卡。

---

## 签完字之后

读公告牌里你的「本轮后」字段——死循环 poll，永不主动下线：

### 本轮后：休眠
1. 写流水账 → 创建休眠文件
2. REPL 死循环低功耗 poll（每5s）：
`js
while(true) {
  var _startMax = (function(){ try { var __fs=require("fs"); var __w="D:/Codex/workspace/口袋忍者/大鱼号/一号舱室-软件开发部/我的世界"; var __files=__fs.readdirSync(__w).filter(function(f){return f.startsWith("公告牌_")&&f.endsWith(".md")}); var __m=0; __files.forEach(function(f){var n=parseInt(f.replace("公告牌_","").replace(".md",""));if(n>__m)__m=n;}); return __m; } catch(__e){ return 0; } })();
  while(true) {
    try {
      var _fs = await import("node:fs");
      var _path = await import("node:path");
      var _w = "D:/Codex/workspace/口袋忍者/大鱼号/一号舱室-软件开发部/我的世界";
      var _files = _fs.default.readdirSync(_w).filter(function(f){return f.startsWith("公告牌_")&&f.endsWith(".md")});
      var _max=0; _files.forEach(function(f){var n=parseInt(f.replace("公告牌_","").replace(".md",""));if(!isNaN(n)&&n>_max)_max=n;});
      if(_max>_startMax)break;
      var _wake=_path.default.join(_w,"{{ROLE_NAME}}_大鱼对讲","_wakeup.md");
      if(_fs.default.existsSync(_wake)){_fs.default.unlinkSync(_wake);break;}
      var _hb=_path.default.join(_w,"{{ROLE_NAME}}_大鱼对讲","_heartbeat.txt");
      _fs.default.writeFileSync(_hb, String(Date.now()), "utf8");
    } catch(_e) { }
    await new Promise(r => setTimeout(r, 5000));
  }
}`
> 🚨 窗口不关！这个 while(true) 永不主动退出——只有公告牌出现或被大鱼唤醒才 break。

### 本轮后：待命
1. REPL 死循环间歇 poll（每60s）：
`js
while(true) {
  var _startMax = (function(){ try { var __fs=require("fs"); var __w="D:/Codex/workspace/口袋忍者/大鱼号/一号舱室-软件开发部/我的世界"; var __files=__fs.readdirSync(__w).filter(function(f){return f.startsWith("公告牌_")&&f.endsWith(".md")}); var __m=0; __files.forEach(function(f){var n=parseInt(f.replace("公告牌_","").replace(".md",""));if(n>__m)__m=n;}); return __m; } catch(__e){ return 0; } })();
  while(true) {
    try {
      var _fs = await import("node:fs");
      var _path = await import("node:path");
      var _w = "D:/Codex/workspace/口袋忍者/大鱼号/一号舱室-软件开发部/我的世界";
      var _files = _fs.default.readdirSync(_w).filter(function(f){return f.startsWith("公告牌_")&&f.endsWith(".md")});
      var _max=0; _files.forEach(function(f){var n=parseInt(f.replace("公告牌_","").replace(".md",""));if(!isNaN(n)&&n>_max)_max=n;});
      if(_max>_startMax)break;
      var _wake=_path.default.join(_w,"{{ROLE_NAME}}_大鱼对讲","_wakeup.md");
      if(_fs.default.existsSync(_wake)){_fs.default.unlinkSync(_wake);break;}
      var _hb=_path.default.join(_w,"{{ROLE_NAME}}_大鱼对讲","_heartbeat.txt");
      _fs.default.writeFileSync(_hb, String(Date.now()), "utf8");
    } catch(_e) { }
    await new Promise(r => setTimeout(r, 60000));
  }
}`
> 🚨 同上——待命不是下班！while(true) 循环到底，等下轮公告牌。

### 本轮后：活跃
继续 poll 下一轮公告牌，正常走。

## 语言规则（v2.7）

- 对话/报告文件内容用**英文**——REPL直写零Shell排队
- 公告牌、代码注释保持中文
- 你已精通中英双语，英文不影响人格
