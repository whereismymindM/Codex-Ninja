# 老渣文档（写公告牌/盯场用）

> 本目录集中放**老渣（人）看的文档**——写公告牌、组队、盯场时用。角色/大鱼模板在 `../模板/`，工具脚本在 `../` 根（scaffold 分发用）。
> 索引：先看「该看哪个」表，按场景选。

---

## 该看哪个（按场景）

| 场景 | 看这个 |
|---|---|
| **唯一填空模板**（格式定稿 + 硬标准 + 照抄） | `公告牌标准模板.md`（照填即可） |
| **赶时间照抄**（一张牌长什么样） | `公告牌标准模板.md`（填空式抄写本） |
| **系统学习**（状态机/扣留/追加/常见错误） | `公告牌完全指南.md`（完整参考） |
| **多轮编排思路**（状态矩阵/角色接力） | `完整示例_RSS摘要系统.md`（⚠️历史示例：字段格式过时，只学编排思路） |
| **小项目单窗口换帽子** | `_Multi-pass_solo.md` |
| **收工后问大鱼体验** | `大鱼侧调查.md`（走对讲目录，放 我的世界/大鱼_老渣对讲/） |

---

## 文档关系

```
公告牌标准模板.md  = 唯一填空模板（硬标准标注 + 六种轮型样例 + 填表规则 + 自查清单）
公告牌完全指南.md  = 为什么（机制原理：状态机/扣留/追加/容错）
完整示例RSS       = 编排示范（历史，仅学思路）
```

> 优先级：标准模板（照填）> 完全指南（原理）。冲突时以 `公告牌标准模板.md` 的硬标准为准。
