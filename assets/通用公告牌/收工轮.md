```
# 公告牌 第00M轮
🔒 第一原则：最后一个动作必须是工具调用，不能纯文字下线；poll 到收工轮才合法退场
- 模式: 收工
- {角色A}（状态：退场）
- {角色B}（状态：退场）
- {角色C}（状态：退场）
- 任务: 全员退场。**收工顺序（goal 模式 007 轮实测定稿）：①写流水账（回顾全程≥2行）→②创建退场文件（对讲目录，touch+ls 确认）→③touch .reasonix-done（角色目录 CWD，touch+ls 确认）→④输出「项目完成」→⑤看宿主反馈：若报 "Goal signaled complete but issues remain"（缺 evidence）→ 补 <autoresearch-evidence> 块（objective_evidence=文件事实 + verification=命令结果，相对路径）→ 标注 [goal:complete] 配合收口（仅限收工轮收口流程，干活轮仍禁止）；若宿主直接收口 → 无需补标 → ⑥回合收口（信号被平台消费删除 = 预期）**。
```

---
