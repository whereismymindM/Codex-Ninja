# 公告牌完全指南

> 读者：你（使用 Skill 的 AGENT）| 一份文件学会写公告牌——格式、状态机、多轮规划、常见错误，全在这里。

---

## 一、公告牌是什么

公告牌是你和项目之间的唯一接口。你写什么，项目就跑什么。大鱼不改一字搬运，角色逐行解读执行，monitor 全自动检查。

一个公告牌 = **一个独立文件** = 一轮任务。

**文件命名必须是 `公告牌_NNN.md`**（NNN=3位补零，如 `公告牌_001.md`、`公告牌_002.md`）。全部放大鱼目录（`火影-大鱼/`）下，大鱼校验后全量发布到 我的世界/，不改一字。

> 🚨 **禁止多轮写进同一个文件！** 大鱼按 `公告牌_001.md` → `公告牌_002.md` → … 编号逐个发布。一个文件塞多轮 = 编号不连续 = 大鱼校验打回 = 项目卡死。

---

## 二、基本格式

NNN=3位补零（如001、002）。

> ⚠️ **铁律：每轮公告牌必须列出项目的所有参与人员，不管本轮是否活跃。** 不活跃的标 `状态：休眠` 或 `状态：待命`。漏写任何人 = 那个角色收不到任何信号 = 可能误判项目结束而退场。

```
# 公告牌 第NNN轮
- 产出类型: [文档 | 代码]
- 模式: [双人对话 | 主笔审核 | 辩论 | 单人输出 | 待命 | 收工]
- [角色名]（角色：[问方|答方|主笔|审核|正方|反方|裁判|单人]，搭档：[搭档名]，状态：[活跃|待命|休眠|退场]，本轮后：[休眠|待命|活跃]，等通知）
- [角色名]（状态：[活跃|待命|休眠|退场]，本轮后：[休眠|待命|活跃]）
- ⚠️ 警告: [可选——本轮特殊注意，如禁止某操作/提醒编码坑/依赖前置；角色 poll 到第一眼必读]
- 任务: [大白话写清楚谁干什么、产出什么、放哪]
- 产出负责人: [角色名]
- 产出: 我的世界/产出/任务NNN_XXX/文件名.md
- 任务目录: 我的世界/任务NNN_XXX/
```

> **产出路径必须指向 `我的世界/产出/` 目录。** `deliver()` 把 `.ready` 信号写在这里，monitor 也只在这里检查。文档类产出需角色先自行 `fs.writeFileSync` 写入内容文件。写错路径（如指向源文件目录 `soulforge/`）= monitor 找不到 `.ready` = 推进判定不通过（⚠️ 仅格式 A（带文件名）有 fallback 兜底；格式 B（目录）无 fallback，会永久 WAIT——正确路径是确保推进的最佳方式）。

---

## 三、核心字段速查

| 字段 | 必填 | 写错了会怎样 |
|------|:--:|-------------|
| 产出类型 | 收工轮不需要 | 收工报告归类错误 |
| 模式 | 是 | 角色走错玩法 |
| 角色名（状态, 本轮后） | 是 | 角色行为随机 |
| 列出全部参与人员 | 是 | 漏掉的成员收不到信号，可能误退场 |
| 任务 | 是 | 产出对不上预期 |
| ⚠️ 警告 | 可选 | 本轮特殊注意（如禁止某操作/编码坑/依赖前置）——角色 poll 到第一眼必读；收工轮不需要 |
| 产出负责人 | 是 | 收工报告追责链断了 |
| 产出 | 是 | monitor 死等不存在的路径 |
| 任务目录 | 收工轮不需要 | 角色不知道去哪交换文件 |
| 辩论轮数 | 辩论模式必填 | 角色不知道辩几轮就停不下来 |

状态值：`活跃`=干活，`待命`=15s轮询（--standby），`休眠`=3s高频poll（不关窗），`退场`=收工轮终局。
本轮后：`休眠`=写休眠文件+切3s高频，`待命`=切15s轮询（--standby），`活跃`=准备接棒。

> ⚠️ 两套间隔别混：**poll 间隔** = bash 循环里 `sleep` 的时长（待命 15s / 休眠 3s）；**心跳间隔** = `_reasonix_poll.js` 写 `_hb_state.json` 的频率（待命 --standby 30s / 休眠 15s）。心跳只是防 2min 超时被砍，不是 poll 频率。

---

## 四、状态机

写公告牌最难的不是格式，是角色状态在轮次间的流转：

```
  待命 --> 活跃 --> 本轮后：待命 --> 待命（继续poll）
   |        |
   |        +--> 本轮后：休眠 --> 休眠（写已休眠_NNN，低功耗poll）
   |                                      |
   |                              被唤醒 --> 活跃
   |
   +-- 待命中被唤醒 --> 活跃（大鱼 _wakeup.js 对所有状态生效）
   |
   +-- 休眠中检测到收工公告牌 --> 写 已退场_NNN（终局）
   |
   +-- 全程待命 --> 收工轮 --> 写 已退场_NNN（终局）
```

三条铁律：
1. **休眠只写一次（除非被唤醒）**。002 休眠写 已休眠_002，003/004 延续休眠不再写。
2. **本轮后和下一轮状态必须对得上**。反过来推：先想每轮谁干活，再倒推本轮后。
3. **待命不是休眠**。待命=随时接棒，休眠=只等唤醒/收工。

### 待命轮 vs 收工轮（决定角色收尾行为）

**角色看到最后一张公告牌后的行为，取决于大鱼发布到 `我的世界/` 的公告牌里最后一张是什么**（角色 poll 的是 `我的世界/公告牌_NNN.md`，不是你的源目录；收工轮被大鱼扣留时，发布里的最后一张 = 待命轮）

| 大鱼发布的最后一张牌 | 角色行为 |
|---|---|
| **待命轮**（`模式: 待命`，全员 `等通知`）| **持续轮询等待新任务通知**——不下线、不退场，随时可被追加任务唤醒 |
| **收工轮**（`模式: 收工`，全员退场）| **写退场文件，退场**——不再 poll，回合结束 |

> 待命轮 = 告诉角色"别走，可能还有活"；收工轮 = 告诉角色"干完了，下班"。

**你写牌规则（待命轮放哪、什么时候放）：**

- **写牌时最后一轮永远是收工轮**（你写的公告牌序列最后一张 = 收工轮）
- **可能要追加** → 在收工轮**前一张（倒二）放待命轮**：`任务轮 → 任务轮 → ... → 待命轮 → 收工轮`
- **确定不追加** → 不放待命轮：`任务轮 → ... → 收工轮`
- **什么时候放待命轮**：**你觉得后面可能还要追加任务时**（项目可能继续、需求可能变）；确定收尾就不放（角色干完看到收工轮自动退场，省资源）

**收口**：有待命轮时，大鱼会扣留收工轮暂时不发，将其余公告牌（含待命轮）一次性发往角色们的工作目录（我的世界），角色做完任务后会在待命轮等通知，而不是下线或退场；待命轮就位 **20 分钟**后（基线：从**前一轮完成时刻**起算——前一轮最后一个 `完成_NNN.md` 的 mtime，无签字时退回用待命轮公告牌 mtime；给你从"角色干完"起的完整追加窗口）仍无新动作，大鱼补搬收工轮，角色 poll 到即退场。

**追加机制（2026-08-04 澄清）**：你要追加任务时：①**写好追加轮**公告牌 → ②**先放大鱼目录 `追加任务.md`**（告知大鱼"追加中，目录不完整是正常的"）→ **再拿走收工轮**（信号先就位，收工轮消失不误判）→ ③把【追加轮 + 待命轮（如果后续还要追加任务则放） + 新收工轮(改好编号)】**一次性放回**大鱼目录 → ④大鱼校验后按正常程序发布（有新待命轮 → 扣新收工轮；无 → 一次性全发）。**待命轮放不放 = 这次追加后还想不想再追加**：放 = 追加后可能还要再追加（角色干完到待命轮持续等，可再走追加流程）；不放 = 这次追加完收口（角色干完 poll 到收工轮自动退场）。`追加任务.md` 被清理 = 追加结束。可多次追加。

---

## 四·5、流程体验报告（可选收尾，可用可不用）

> 生产项目收尾前，你**可选**加一轮"流程体验报告"——角色各自写反思分析，作为生产即测试的反馈（不强制，你想收集体验时才用）。

**公告牌模板**（你贴用，填 NNN/角色）：

```
# 公告牌 第NNN轮（流程体验报告）
- 模式: 单人输出
- [角色]（状态：活跃，本轮后：待命）
- ⚠️ 警告: 体验报告 ≠ 流水账——流水账是事实记录（每轮干了什么/等了谁），体验报告是反思分析（卡点/坑/优化建议）。先读自己的流水账回忆事件，再提炼分析。
- 任务: 每个角色写一份 `{角色}_流程体验报告.md` 到 产出/任务NNN_流程体验/：
  ①遇到的卡点（按严重度排序：环节/现象/影响/怎么恢复的）
  ②踩过的坑（指令歧义/工具坑/等待协作问题，写具体现象）
  ③可怎么优化（对模板/公告牌/工具的具体建议，按性价比排序）
  ④做对了的/值得保留的（正面反馈）
- 产出负责人: [角色]
- 产出: 我的世界/产出/任务NNN_流程体验/{角色}_流程体验报告.md
- 任务目录: 我的世界/任务NNN_流程体验/
```

**分工**：流水账 = 素材（事实），体验报告 = 加工（分析）；角色写报告前先读自己流水账回忆事件。大鱼收工审计时可顺带汇总各角色体验报告 → "本轮项目改进点"清单。

不要直接写公告牌。先画状态矩阵：

| 角色 | 001 | 002 | 003 | 004(收工) |
|------|-----|-----|-----|-----------|
| DHH | 活跃->待命 | 活跃->休眠 | 休眠 | 退场 |
| 林纳斯 | 待命 | 活跃->休眠 | 休眠 | 退场 |
| 图灵 | 待命 | 待命 | 活跃->休眠 | 退场 |

粗体=活跃，箭头后=本轮后。写好这个表再逐轮翻译成公告牌文件。

> ⚠️ **前置依赖规则（全量发布必读）**：若某轮任务依赖上一轮（或更早）的产出，**任务描述必须显式写明依赖文件路径**，例如"读 `产出/任务001_需求/需求文档.md`，然后……"。角色会先等该文件就位再干活（模板已含等前置铁律）。**不写依赖 = 角色可能不等直接干 = 产出对不上**。

---

## 六、产出路径写法

monitor 根据产出行的写法自动切换两种检查模式：

**格式A（推荐）：带文件名**
```
- 产出: 我的世界/产出/任务001_引擎/server.js, search-engines.js
```
monitor 在产出目录下逐项检查 `server.js.ready` 和 `search-engines.js.ready`。deliver() 交付时自动在同目录生成 .ready 信号——两者一一对应。全部 .ready 就位才判定通过。

**格式B：仅目录名**
```
- 产出: 我的世界/产出/任务001_引擎/
```
monitor 对目录做 readdirSync 扫描，存在任意 .ready 文件即通过。

> **产出路径必须指向 deliver() 写入的产出目录（即 `我的世界/产出/任务NNN_XXX/`）。**
> 指向源文件所在目录（如 `我的世界/soulforge/`）会导致 monitor 先去错误位置找 .ready，找不到再走 fallback 扫描 `产出/` 子目录兜底（⚠️ 仅格式 A 场景；格式 B 无 fallback）。能跑通，但多一次扫描——正确路径直接命中，判定更快。

多个产出目录用多行写：
```
- 产出: 我的世界/产出/任务001_前端/index.html
- 产出: 我的世界/产出/任务001_后端/server.js
```
### 产出类型：代码 vs 文档

你写产出路径之前，先判断本轮产出属于哪种类型——这决定了角色怎么交付、monitor 怎么检查：

| | 代码类产出 | 文档类产出 |
|---|---|---|
| **场景** | 修改已有源文件（如 soulforge/extractImports.js） | 从零新建（报告、设计文档、裁判结论等） |
| **角色怎么做** | 在源文件目录原地改完 → 直接 await deliver("文件名", "任务NNN") | 先 fs.writeFileSync 写到 产出/ → 再 await deliver() |
| **deliver() 行为** | 只写 .ready 信号，不搬运文件 | 角色已把内容写入产出/，deliver() 补 .ready 信号 |
| **文件实际在哪** | 项目源码仓库（如 soulforge/） | 产出/任务NNN_XXX/ |
| **公告牌产出路径** | 我的世界/产出/任务NNN_XXX/文件名（指向信号位置） | 我的世界/产出/任务NNN_XXX/文件名（文件+信号都在这） |
| **Monitor 行为** | 不变——始终扫 产出/ 目录找 .ready | 不变 |

> **公告牌的产出路径永远写 我的世界/产出/任务NNN_XXX/。** 不管代码类还是文档类——信号都在这里。角色根据模式文件里的说明判断自己是代码类还是文档类，选择对应的交付方式。

## 七、模式范例

### 双人对话（一问一答）

```
# 公告牌 第001轮
- 产出类型: 文档
- 模式: 双人对话
- 产品经理-乔布斯（角色：问方，搭档：架构师-林纳斯，状态：活跃，本轮后：待命）
- 架构师-林纳斯（角色：答方，搭档：产品经理-乔布斯，状态：活跃，本轮后：待命）
- 前端开发-尤雨溪（状态：待命，本轮后：活跃）
- 任务: 乔布斯跟林纳斯对话深挖需求，林纳斯产出需求文档
- 产出负责人: 架构师-林纳斯
- 产出: 我的世界/产出/任务001_需求挖掘/需求文档.md
- 任务目录: 我的世界/任务001_需求挖掘/
```

### 主笔审核（一写一审）

```
# 公告牌 第002轮
- 产出类型: 代码
- 模式: 主笔审核
- 产品经理-乔布斯（状态：休眠）
- 架构师-林纳斯（角色：主笔，搭档：前端开发-尤雨溪，状态：活跃，本轮后：休眠）
- 前端开发-尤雨溪（角色：审核，搭档：架构师-林纳斯，状态：活跃，本轮后：休眠）
- 任务: 林纳斯写系统设计，尤雨溪审核
- 产出负责人: 架构师-林纳斯
- 产出: 我的世界/产出/任务002_系统设计/系统设计.md
- 任务目录: 我的世界/任务002_系统设计/
```

### 辩论（正反对抗 -> 裁判结论）

> **5-2 声明（2026-08-04 第五轮实测）**：辩论的**文件命名与流程以公告牌任务描述为准**（第五轮实测：公告牌"立场→反驳→总结" vs 玩法文件"立论→找茬→总结陈词"两套命名，角色靠猜"公告牌优先"兜底——不该让角色猜）。公告牌任务里写清每步文件名（辩论_01_xx/02/03），角色严格照做；玩法文件是通用参考。

```
# 公告牌 第004轮
- 产出类型: 文档
- 模式: 辩论
- 架构师-林纳斯（角色：正方，搭档：后端开发-DHH，状态：活跃，本轮后：休眠）
- 后端开发-DHH（角色：反方，搭档：架构师-林纳斯，状态：活跃，本轮后：休眠）
- 质量审计-图灵（角色：裁判，状态：活跃，本轮后：休眠）
- 产品经理-乔布斯（状态：休眠）
- 前端开发-尤雨溪（状态：休眠）
- 任务: 林纳斯(正方)主张monolith，DHH(反方)主张微服务。图灵裁判给结论。
- 辩论轮数: 3
- 产出负责人: 质量审计-图灵
- 产出: 我的世界/产出/任务004_技术选型辩论/裁判结论.md
- 任务目录: 我的世界/任务004_技术选型辩论/
```

### 单人输出（独立交付）

```
# 公告牌 第003轮
- 模式: 单人输出
- 产品经理-乔布斯（状态：休眠）
- 架构师-林纳斯（状态：休眠）
- 前端开发-尤雨溪（角色：单人，状态：活跃，本轮后：休眠）
- 任务: 尤雨溪写前端页面
- 产出负责人: 前端开发-尤雨溪
- 产出: 我的世界/产出/任务003_前端开发/index.html
- 任务目录: 我的世界/任务003_前端开发/
```

### 收工（全员退场）

```
# 公告牌 第005轮
- 模式: 收工
- 产品经理-乔布斯（状态：退场）
- 架构师-林纳斯（状态：退场）
- 前端开发-尤雨溪（状态：退场）
- 任务: 全员退场。写流水账->创建退场文件->输出「项目完成」结束回合（窗口常驻不关，随时可继续对话）。
```

> 收工轮只写 模式 + 角色名（状态：退场）+ 任务，产出类型、角色、搭档、本轮后、产出、任务目录、产出负责人全部省略。


## 八、容错机制（使用者须知）

多角色协作中搭档可能掉线。系统有三层容错：

1. **心跳自愈**：角色轮询每轮写心跳时间戳，monitor 2 分钟无心跳自动写唤醒文件。角色通过 bash while + _reasonix_poll.js 持续轮询保活。
2. **死锁并行处理**：等搭档文件超时（双人对话/主笔审核 20 分钟），角色同时做两件事——写 `_deadlock.md` 通知 monitor 尝试唤醒搭档，但自己不卡等，跳过缺失步骤继续推进。**辩论除外**——裁判是天然兜底，辩论超时不写 `_deadlock.md`，只 break 继续。裁判即使只拿到一方材料也能给结论。
3. **产出兜底**：monitor 推进判定不看签字只看产出就位——签字不阻塞推进，但**角色必须签字**（收工审计的逐轮完成矩阵会核对签字文件，缺失标红）。

> 💡 这意味着你设计多轮方案时，可以放心用双人模式——搭档掉线不会卡死整轮。

## 九、常见错误

1. **已经休眠的角色重复写本轮后**——休眠状态不需要本轮后，角色已经在低功耗 poll 了。
2. **本轮后跟下一轮状态对不上**——002 轮说干完待命，003 轮却是退场。
3. **收工轮写了产出和任务目录**——monitor 只看退场文件。
4. **忘写产出负责人**——monitor 不报错但收工报告追责链断了。
5. **任务描述里出现角色名（状态：活跃）格式**——⚠️ 已修复（monitor 用 headerPart 限定扫描区，任务描述不再被误抓，见隐患清单 #9）；此处保留为历史注意事项，不再构成实际风险。
6. **产出路径没从 我的世界/ 开始**——产出正则要求 `产出: 我的世界/...` 前缀，无前缀**完全匹配不到**（outputCount=0）→ 活跃轮永久 WAIT，**没有 fallback**。务必按模板写 `产出: 我的世界/产出/...`。
7. **辩论模式忘了写辩论轮数**——角色无法确定自由辩论多少轮，行为不可预测。
8. **双人对话没区分谁问谁答**——只有问方能写 `对话结束.signal` 喊停，角色字段里必须标注。
9. **多轮写进一个文件**——大鱼只按文件名 `公告牌_NNN.md` 发布，一个文件塞多轮 = 项目卡死。每轮必须独立文件。
10. **漏写非活跃角色**——每轮必须列出项目全部参与人员，不管本轮是否干活。漏写 = 角色收不到信号 = 可能误退场。

---

## 十、推荐流程

动手之前，先做一轮模式判断——这决定了填模板时选什么模式、产出类型、路径怎么写：

| 场景 | 模式 | 产出类型 | 产出路径 |
|---|---|---|---|
| 一个人独立写代码/文档 | 单人输出 | 代码类（改源文件）/ 文档类（新建） | 我的世界/产出/任务NNN_XXX/文件名 |
| 一问一答深度挖掘 | 双人对话 | 文档类（对话精华报告） | 我的世界/产出/任务NNN_XXX/报告.md |
| 一人写、一人审 | 主笔审核 | 代码类/文档类（看审的是什么） | 我的世界/产出/任务NNN_XXX/文件名 |
| 正反辩论、裁判给结论 | 辩论 | 文档类（裁判结论） | 我的世界/产出/任务NNN_XXX/裁判结论.md |
| 全员退场 | 收工 | 无 | 不需要产出字段 |

> **代码类 → 角色原地改完直接 deliver() 发信号。文档类 → 角色先写内容到产出/，再 deliver()。路径永远写 我的世界/产出/。**

1. 画状态矩阵——谁哪轮干活、干完什么状态
2. 定产出和路径——每轮出什么、放哪、谁负责
3. 逐轮翻译——按模板把状态表转成公告牌
4. 自查：本轮后跟下轮状态一致？收工轮没多写产出？任务描述里有 monitor 陷阱？
5. 放大鱼目录下，编号连续

> 公告牌就是代码。角色 Agent 逐行解析，没有中间态。写完之后对着状态矩阵逐条核对。

> 💡 看完还不会写？参考 完整示例_RSS摘要系统.md——5 角色 6 轮完整公告牌。
