# 大鱼实测教训（运行时沉淀）

> 本文件记录**你这个项目里实测踩过的坑**——每条 = 当前正确做法。**只留当前有效条目**：新坑进、旧坑出，过时的删除（防止变成教训累积容器）。
> 格式：现象一句话 + 正确动作。**不记编号、不记"哪轮改的"**——执行者只需要知道"现在该怎么做"。
> 维护时机：每轮收工复盘时，把踩过的坑沉淀进来；项目结束清理。

---

## 当前教训（按踩坑时间倒序）

- （空——项目开始后，每轮复盘把坑沉淀到这里）

---

## 沉淀示例（格式参考，写后删除）

- 现象：查角色进度用 `ls | tail` 看最后几行，结果漏看了 T13（字典序 T10<T13<T9，tail 截到 T9）
  正确动作：`ls -t <目录> | head` 按 mtime 排序看最新
- 现象：写心跳时忘了先查对讲目录，老渣发的任务悬空
  正确动作：每回合必做 = 拉日志 + 查对讲 + 刷心跳 + 决策（见 AGENTS.md 周期验证节）
