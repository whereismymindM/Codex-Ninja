# codex-ninja 审查清单（易错点沉淀 · 两大类）

> 历轮实测/复核沉淀的高频易错点，**先分清"谁出错"再查**——
> **一、Skill 本身错误**（工具/模板/文档缺陷，修 Skill）；**二、角色行为错误**（AI 用工具/理解设定时容易犯，改约束或引导）。
> 每条标来源轮次。审查前先读本清单逐项过；发现新易错点 → 按类追加。

---

## 一、Skill 本身错误（修 Skill）

> 这类是 **codex-ninja 的缺陷**——角色照做就会错，或工具实现有 bug。审查重点：查代码/模板/文档本身。

### 1. 角色约束语句/模板表述错误（AGENTS.md / 玩法文件的文字缺陷）

- [ ] **参数语义写错**：文档对脚本参数的描述必须与实现一致（10-6：`--loop N` 文档写"当前轮号"实际是"循环次数"——真 bug；10-5：poll 传参"当前N"歧义致 lastN off-by-one）
- [ ] **调用示例与实现不一致**：模板/玩法文件写的调用方式必须实测可用（9-4：wait_file 支持 `--any` 但玩法文件没指引 → 角色 AND 等卡 10 分钟）
- [ ] **悬空指涉**：文档引用的代码块/脚本必须真实存在（8-3/10-6："等文件内联轮询"代码块指涉悬空）
- [ ] **全角/半角混用**：字面匹配类字段（模式/警告）全文统一半角+空格，**grep 全角冒号查残留**（10-8：:117/:152 全角"模式：收工"残留，角色字面匹配失配）
- [ ] **占位符 vs 实例**：模板示例的文件名/角色名必须用 `{{ROLE_NAME}}` 或注明"以自己名字开头"（10-6：结束标记示例缺角色名前缀，3 人同提）
- [ ] **铁律自相矛盾/自我削弱**：命令式+但书结构易冲突（8-3/10-6：铁律 4/5/9 .tmp vs 原生 write_file 矛盾，统一为判定条件式）
- [ ] **状态流转字段**：公告牌"本轮后"必须 = 下一轮状态（第十一轮大鱼校验打回：本轮后：待命 vs 下轮活跃）
- [ ] **边界未定义**：如"轮询方式三套并存无判据"（10-7 图灵：普通轮 poll 一次/待命轮 while/--loop 何时用——已补判据）
- [ ] **占位符与语义口径不统一**：改了参数语义但占位符没跟上（11-1：10-5 改 lastN 语义但 `<当前N>` 残留 4 处——**改语义必 grep 旧占位符**）
- [ ] **孤立行号引用**：模板写 ":144" 跨文档行号必漂移（11-1 纳特 A1）——**用具名引用（节名/文档名）**，禁裸行号
- [ ] **"已并入"声明 vs 逐条标注并存**：声明与括号标注的关系必须说清（11-1 纳特 A3：声明"已全部并入"vs 20 处括号标注——统一为"括号=修正轨迹注记"）
- [ ] **示例代码可运行性**：示例块注释嵌说明、照抄会执行出错（11-1 纳特 A2）——示例只留可运行骨架，说明放块外

### 2. 代码/脚本错误（工具 bug）

- [ ] **参数解析缺陷**：如 `--standby` 硬编码 args[2]（9-11 N-3：与 --loop 组合顺序敏感——已改 indexOf）；`--help` 被当路径（10-3）
- [ ] **输出重复/日志膨胀**：如 monitor WAIT logMonitor 调用 2 次（9-11 N-2）、poll 铁律提示重复 2 遍（9-11 N-1）
- [ ] **功能静默失败**：replace 的字符串不存在没报错（9-11 补：BULLETIN 提示原 replace NEW_BOARD 不存在未生效）
- [ ] **能力半成品**：脚本支持了参数但文档/指引没跟上（9-4/10-3：wait_file --any 支持了但玩法文件没指引）

### 3. 公告牌写法错误（老渣写牌）

- [ ] **产出字段花括号枚举**：`{A,B}.md` monitor 不认，按字面找单文件 → OUTPUT missing 卡收尾（第七轮我踩过）——**用逗号分隔**
- [ ] **产出路径与玩法文件不一致**：公告牌写"产出/任务XXX" vs 玩法文件写"任务目录" → 双方按不同路径互相等（9-5：卡 12 分钟）——**公告牌任务描述不得另写路径覆盖玩法文件口径**
- [ ] **任务目录字段冗余**：单人输出轮无交换区，任务目录字段多余（第十轮纳特体验报告）
- [ ] **公告牌源位置**：放大鱼目录根平铺，不建子目录（第十轮大鱼 ls 匹配不到）

---

## 二、角色行为错误（AI 用工具/理解设定时容易犯）

> 这类是 **角色（AI）的执行偏差**——工具/模板本身正常，是角色用错/理解错。审查重点：查角色约束是否写清、是否需要引导。

### 交付/信号类（文件握手协议执行遗漏）

- [ ] **写完产出忘 deliver**（缺 `.ready`）→ 搭档/乔布斯等不到（10-3：纳特 003 漏 deliver 卡乔布斯）——deliver 必须生成 `.ready`
- [ ] **写完文件忘写 `.signal`** → 搭档不知道你写好了，永远等
- [ ] **处理完 signal 忘 ack**（不改 `_acked`）→ 下次 poll 重复处理/误判
- [ ] **产出写错目录**（任务目录 vs 产出目录混）→ 搭档按玩法文件找错位置（9-5/第七轮 006）

### 轮次/推进类（理解偏差）

- [ ] **poll 传参 lastN 算错**（把"下一张"当 lastN）→ 跳号（10-5：SIGN 002 后传 3 → poll 检测 004 → 跳过 003）
- [ ] **正方先手误等反方**：自由辩论正方先手**独立写 T1，勿等任何人**——角色把"等搭档"习惯套用到"该自己先手"（第十一轮大鱼干预点破；玩法文件 :139 本有先手规则但角色没执行——**提示要显眼+实测教训绑定**，11-1 强化）
- [ ] **"20 分钟"理解成上限**：实际是常见扣留值非上限，可能更长，继续 poll（10-6）
- [ ] **收工判定自行判断**：应等 poll 退出码 2，不主动按字面"模式: 收工"建退场文件（8-3）

### 回合/下线类（最频发）

- [ ] **口头声明 = 下线**："我干完了/我在盯着/我不下线"输出即回合结束（8-1/9-9）——保持在场 = 持续工具调用，不是表态
- [ ] **人工介入后习惯性下线**：回应对话后必须继续工具调用（9-6/10-6 量化：回应后必须跟一次工具调用）
- [ ] **把"等搭档"当"该自己先手"**（同正方先手）——活跃轮该干就干，不等别人
- [ ] **非收工轮结束回合不建标记**：违规结束须先建 `{角色}_结束回合_<NNN>.md`（3 位补零，10-1/10-2）
- [ ] **重启回传上次 poll 值**：会话重启后 lastN 从自检重算，禁止回传旧上下文（11-1 乔布斯——10-5 跳号的跨会话版本）

### 工具使用类

- [ ] **CWD 偏移**：cd 进 临时脚本 再跑 `../我的世界` 路径全错（9-1）——角色目录 CWD 下跑
- [ ] **wait_file 双文件忘 --any**：等"任一"场景必须 `--any`（9-4/10-3）
- [ ] **等文件续心跳**：手写循环每 30s 写心跳（5-10/8-3）；wait_file 用 --hb

---

## 新增易错点记录区（审查时发现 → 追加，并归到上述两类）

| 日期 | 易错点 | 归属（Skill/角色）| 来源 |
|---|---|---|---|
| 2026-08-05 | 全角/半角字面匹配失配 | Skill（模板）| 第十轮乔布斯①3 + 10-8 |
| 2026-08-05 | --loop 参数语义写错 | Skill（模板）| 第十轮图灵 2 |
| 2026-08-05 | poll 传参 off-by-one | Skill（模板表述）+ 角色理解 | 第十轮大鱼建议 + 10-5 |
| 2026-08-05 | 正方先手误等反方 | 角色行为 | 第十一轮大鱼干预 |
