# codex-ninja 审查清单（易错点沉淀）

> 历轮实测/复核沉淀的**高频易错点**——审查 Skill 时逐项核对，防止遗漏。
> 来源：第一~十轮实弹测试 + 外部复核（N-1~N-3、10-7/10-8 等）+ 角色自检报告。
> 用法：审查前先读本清单，逐项过；发现新易错点 → 追加到对应类目。

---

## A. 文档 vs 代码 一致性（最高频，已踩 5+ 次）

- [ ] **参数语义**：文档对脚本参数的描述必须与实现一致（10-6 教训：`--loop N` 文档写"当前轮号"实际是"循环次数"——真 bug）
- [ ] **调用示例**：模板/玩法文件写的调用方式必须与脚本实际用法一致（9-4 教训：wait_file 支持 `--any` 但玩法文件没指引 → 角色 AND 等卡 10 分钟）
- [ ] **功能存在性**：文档引用的代码块/脚本必须真实存在（8-3/10-6 教训："等文件内联轮询"代码块指涉悬空）
- [ ] **全角/半角**：字面匹配类字段（模式/警告等）全文统一半角+空格，禁止全角实例残留（10-8 教训：:117/:152 全角"模式：收工"残留，角色字面匹配失配——**审文档时 grep 全角冒号**）
- [ ] **占位符 vs 实例**：模板示例中的角色名/文件名必须是 `{{ROLE_NAME}}` 或明确"以你的名字开头"，禁止照抄会错（10-6 教训：结束标记示例缺角色名前缀，3 人同提）

## B. 传参/记账（跳号类）

- [ ] **poll 传参语义**：`<lastN>` = 上一张已处理牌号，不是"下一张"（10-5 教训：SIGN 002 后传 3 → poll 检测 004 → 跳过 003 审核）
- [ ] **lastN 推进**：干完一轮 lastN 更新为本轮编号，不是 N+1（同 10-5）
- [ ] **收工判定**：以 poll 退出码 2 为准，不主动按字面"模式: 收工"创建退场文件（8-3）

## C. 路径/目录

- [ ] **CWD 偏移**：所有 `_*.js` 工具在角色目录 CWD 下跑，禁止 cd 进 临时脚本（9-1 教训）
- [ ] **__dirname vs cwd**：wait_file 已内部锚定（__dirname）；自写脚本用 process.cwd()（10-6 消旧铁律冲突）
- [ ] **公告牌源位置**：公告牌源 = 大鱼目录根（平铺），不是子目录（第十轮大鱼 ls 匹配不到教训）
- [ ] **mtime 陷阱**：cp 保留源文件 mtime——公告牌 mtime = 写牌时间 ≠ 发布时间；20 分钟补搬基准 = 前一轮完成时刻（完成_NNN.md mtime）

## D. 权限/边界

- [ ] **角色越权**：角色不能写公告牌/我的世界 根——求助升级不得要求"在公告牌标注"（10-7 教训）
- [ ] **删除对象**：删除申请走求助通道（大鱼），不写"老渣"（10-7 统一）
- [ ] **档案馆**：绝对禁止动（最高优先级，永不例外）

## E. 回合/下线（核心语义）

- [ ] **最终回复 = 回合结束**：判据 = 纯文本无工具调用即最终回复（10-6 补可执行判据）
- [ ] **保持在场 ≠ 脚本在跑**：口头声明"我不下线"= 下线瞬间（8-1/9-9）
- [ ] **收尾点**：DONE + 三件套 = 合法结束回合；退场文件创建 = 角色合法收尾（8-1）
- [ ] **10-1 fail-loud**：非收工轮结束回合须创建结束标记（3 位补零 `<NNN>`），收工轮不创建（10-1/10-2）

## F. 等文件

- [ ] **--any vs AND**：辩论/双人等"任一就位"场景必须 `--any`（9-4/10-3）
- [ ] **--help**：wait_file 支持 `--help/-h`（10-3）
- [ ] **等文件续心跳**：wait_file 自带 --hb；手写循环每 30s 写心跳（5-10/8-3）

## G. 其他高频

- [ ] **待命时长**："20 分钟"是常见扣留值非上限，可能更长（10-6 统一）
- [ ] **铁律自相矛盾**：命令式+但书结构易自我削弱（8-3/10-6 铁律 4/5/9 统一为判定条件式）
- [ ] **时间基准**：先 `date` 查当前时间再判断超时/补搬（第九轮我两次拿 mtime 误判大鱼教训）
- [ ] **角色自检**：新改动后跑自检轮（角色通读 AGENTS.md 挑问题）——是抓"文档落后"的最有效手段

---

## 新增易错点记录区（审查时发现新问题 → 追加这里，并在上面相应类目补勾选项）

| 日期 | 易错点 | 来源 |
|---|---|---|
| 2026-08-05 | 全角/半角字面匹配失配（模式：收工 vs 模式: 收工）| 第十轮乔布斯自检①3 + 10-8 |
| 2026-08-05 | --loop 参数语义文档写错（当前轮号 vs 循环次数）| 第十轮图灵自检 2 |
| 2026-08-05 | poll 传参 off-by-one（lastN vs 下一张）| 第十轮大鱼建议 + 10-5 |
