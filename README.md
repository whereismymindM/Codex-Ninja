# Codex Ninja · 多Agent协作框架

> 给 AI 一个办公室，他们自己开会、干活、下班。
> 当前版本：**初代**

## 这是什么

Codex Ninja 是一个 AI Agent 团队协作框架。通过文件系统通信、公告牌调度、休眠协议自治运行——你交代完任务就可以离开，项目自动推进到完成。

## 三方协作

| 角色 | 窗口 | 职责 |
|------|:--:|------|
| **老渣**（你） | 用户窗口 | 写公告牌、盯场救火、陪故国有明聊天 |
| **大鱼** | 独立窗口 | 搬运公告牌 → 监控翻篇 → 唤醒角色 → 收工审计 |
| **各角色** | N 个窗口 | 轮询公告牌 → 干活 → 签字交付 → 休眠/待命 |

## 四种玩法

| 模式 | 角色 | 适用场景 |
|------|------|----------|
| 单人输出 | 1人 | 写代码、写文档、独立任务 |
| 双人对话 | 问方+答方 | 需求访谈、方案讨论 |
| 主笔审核 | 主笔+审核 | 代码审查、文档校对 |
| 辩论 | 正方+反方+裁判(可选) | 技术选型、架构决策 |

## 容错能力

> 角色可能在任意时刻掉线。系统有三层容错确保项目不卡死：

| 层级 | 机制 | 怎么工作的 |
|:--:|------|------|
| 1 | **心跳自愈** | 角色每轮写心跳时间戳，monitor 2分钟无心跳自动唤醒；轮询 240 秒主动重生，不依赖外部续命 |
| 2 | **死锁并行** | 等搭档文件超时写死锁信号通知 monitor + 自己继续推进，两条恢复路径同时跑 |
| 3 | **产出兜底** | monitor 翻篇只看产出就位不看签字，签字失败不阻塞项目推进 |

## 核心机制

- **公告牌驱动**：任务写在 `公告牌_NNN.md`，大鱼逐轮搬运，角色轮询读取——不需要长连接
- **角色状态机**：待命 → 活跃 → 休眠 → 退场，公告牌指定每轮谁干活、干完去哪
- **240s REPL自愈**：轮询循环在 REPL 300s 硬上限前主动退出，AI 自然重发——真正的永动
- **双向心跳**：角色写心跳保活 + monitor 反向检测，任一方离线都能被发现
- **原子操作**：`.tmp → rename` 写所有文件，搭档不会读到半截

## 快速开始

1. 安装 Skill 到 Codex skills 目录
2. `ninja init my-project` 生成角色目录
3. 读 `公告牌完全指南.md`，在 `火影-大鱼/` 下写公告牌（每轮一个独立文件）
4. 开大鱼窗口 + 角色窗口，各输入「进入角色」
5. 离开，让他们自己干活 ☕

## 目录结构

```
codex-ninja/
├── SKILL.md                 # Skill 入口（老渣身份声明）
├── README.md                # 本文件
├── CHANGELOG.md             # 版本历史（时间线格式）
├── assets/                  # 玩法文件 + 模板 + 工具
│   ├── 公告牌完全指南.md      # 老渣写公告牌的完整参考
│   ├── 大鱼_AGENTS模板.md     # 大鱼窗口模板
│   ├── 角色_AGENTS模板.md     # 角色窗口模板
│   ├── _双人对话模式.md       # 玩法流程文件
│   ├── _主笔审核模式.md
│   ├── _辩论模式.md
│   ├── _单人输出模式.md
│   ├── monitor.js           # 大鱼翻篇监控
│   └── _poll.js 等工具       # Shell 备用工具
├── scripts/                 # 脚手架
└── 团队须知/                # 注入所有角色窗口的通用规则
```

## 设计文档

桌面 `codex-ninja.md` 包含完整的设计理念、三方协作时序图、各模式流程图、机制详解和审核检查清单。修改 Skill 前先读它。

## 设计哲学

> 文件系统是总线，公告牌是时钟，大鱼是调度器，休眠是安全网，容错是底线。

- 📋 公告牌驱动：不需要长连接，不需要消息队列
- 🐟 大鱼不干活：调度器和工人严格分离
- 🚪 休眠不是下班：窗口永远不关，while(true) 循环到底
- 💉 灵魂注入：不是通用 Agent，是专家
- 📁 文件系统通信：可审计、可回溯、可恢复
- 🛡️ 三层容错：单点故障不卡死整轮

## 许可证

MIT