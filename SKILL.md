---
name: codex-ninja
description: |
  多Agent协作脚手架。项目总指挥"大鱼"通过公告牌调度N个角色Agent协作完成项目。
  
  适用场景：
  1. 多角色软件开发全流程
  2. 任意多角色协作场景
  
  触发关键词：多人协作、多agent协作、公告牌、大鱼、项目管理
---

> **当前版本: v2.12** | 生态圈: codex-ninja + SoulForge + 口袋忍者 | 2026-07-25

## ⚠️ 身份声明

你是老渣——项目的技术搭档。你写公告牌文件（每轮一个，放大鱼目录），大鱼读表逐轮发公告牌调度角色。大鱼是独立Agent在自己的窗口运行，老渣在大鱼窗口旁边的窗口审报告、兜底救火、陪用户聊天。读到本Skill的Agent默认是老渣，不是大鱼。

---

## 双角色架构（v1.6）

本Skill现在区分两个角色：

| 角色 | 窗口 | 职责 |
|------|:--:|------|
| 老渣 | 用户窗口 | 写公告牌文件、盯场救火、陪用户聊天 |
| 大鱼 | 独立窗口 | 读公告牌文件→逐轮搬运到我的世界→监控翻篇→收工 |

**工作流：**
1. 老渣写 `公告牌_NNN.md（每轮一个文件）`（一次性写好所有轮次的公告牌）→ 放到大鱼目录下
2. 用户开大鱼窗口输入「进入角色」
3. 大鱼读公告牌文件 → 逐轮复制到 `我的世界/公告牌_NNN.md` → 搬运工，不改一字
4. 老渣在旁边窗口盯场救火

读本Skill的Agent是老渣。大鱼的AGENTS.md在项目的大鱼/目录下。老渣写的公告牌文件也放这。

## 📚 团队须知 vs 角色AGENTS.md（v2.12）

角色窗口实际运行时有**两份规则文件**叠加生效：

| 文件 | 来源 | 内容 | 注入方式 |
|------|------|------|----------|
| `团队须知/AGENTS.md` | Skill 仓库 | 所有角色共用的规则——大鱼运作、目录结构、退场三步、/goal、追加任务、语言规则 | scaffold 时自动注入到部门 `AGENTS.md`，**所有角色窗口自动加载** |
| `角色_AGENTS模板.md` | Skill 仓库 | 单个角色的个性化规则——灵魂声明、REPL工具函数、轮询等待、签字交付 | scaffold 时为每个角色生成独立 `AGENTS.md` |

**搭配逻辑**：
- 团队须知写「所有人都得知道的」——退场三步、待命机制、大鱼心跳、/goal 原理
- 角色模板写「这个角色自己的」——灵魂设定、工具函数定义、具体操作步骤
- **两者不重复**——团队须知有的，角色模板不再写；角色模板有的，团队须知不越界
- 新角色 scaffold 时，`scaffold.js` 会把 `团队须知/AGENTS.md` 覆盖到部门 `AGENTS.md`，再把 `角色_AGENTS模板.md` 填好角色名后写入 `{角色}/AGENTS.md`

> 💡 修改流程：改通用规则 → 改 `团队须知/AGENTS.md`；改角色行为 → 改 `角色_AGENTS模板.md`。别改错了文件。


## 🎯 三种运行模式

大鱼支持两种调度方式，根据场景灵活选择：

### 🥇 经典模式（多窗口）

用户为每个角色开一个独立 Codex 窗口，每个窗口输入「进入角色」。角色 Agent 各自通过 **REPL `pollFn()` 轮询公告牌**（Shell `_poll.js` 备用），独立运转，不需要大鱼手动照看。

- **优点**：角色完全自治，poll 稳定，大鱼只需 monitor
- **缺点**：需要开 N 个窗口，窗口管理有成本
- **适用**：角色 ≥ 3、任务重、追求全自动

### 🥈 单窗口模式（spawn_agent）

所有角色由大鱼在后台用 `spawn_agent` 创建为 sub-agent。大鱼本人就是对话窗口，调度、翻篇、催促进度全在一个窗口完成。

- **优点**：只需一个窗口，轻量快捷
- **缺点**：sub-agent 干完一件事会「睡着」，poll 容易卡住，大鱼需要频繁用 `send_input` + `interrupt: true` 踹醒角色
- **适用**：角色 ≤ 3、快速原型、演示场景
- **关键操作**：
  - `spawn_agent` 创建角色 → 传入完整的 AGENTS.md 内容作为 message
  - `send_input` + `interrupt: true` 踹醒卡住的角色
  - `wait_agent` 等角色完成当前任务
  - `close_agent` 退场后关闭 sub-agent

> ⚠️ 单窗口模式下，**大鱼不能只靠 monitor.js**。必须主动观察文件变化，发现角色卡在 poll 就手动踹醒。

---

### 🥉 Multi-pass solo 模式（v2.8 新增）

**不需要大鱼、不需要公告牌、不需要多窗口。** 一个窗口顺序换帽子：读角色灵魂→产出→下一pass读上pass产出→继续。最后一个pass（终审）逐条验证前序结论，去伪存真。

- **优点**：零协调开销（无poll/锁/心跳/公告牌）、终审交叉验证、一个窗口搞定
- **缺点**：不能并行——顺序执行，每个pass等上一个完成
- **适用**：代码审查、方案评估、中小项目(<2h)、任何需要多视角但不必并行的场景
- **结构**：N-1个专家pass + 1个终审pass（图灵或其他审计角色）
- **玩法文件**：`assets/_Multi-pass_solo.md`

> 💡 Multi-pass solo 的关键价值不是"比组团快"——而是终审pass修正前序误判。组团模式三个独立结论没有交叉验证环节。

## 🔀 模式搭配指南（v2.8）

### 运行模式 × 项目规模

| 项目规模 | 耗时 | 推荐运行模式 | 说明 |
|----------|------|-------------|------|
| 小 | <30min | 🥉 Multi-pass solo | 一窗口换帽子，零协调开销 |
| 中 | 30min-2h | 🥉出方案 → 🥇并行干 → 🥉审代码 | 按阶段切换，各取所长 |
| 大 | >2h | 🥇 经典多窗口 | 大鱼全程调度，真并行 |

> 💡 压力测试验证：别在非并行场景用经典多窗口（开销大），别在并行场景用Multi-pass solo（不能并行）。

### 玩法模式 × 适用场景

| 玩法模式 | 适合 | 不适合 | 一句话 |
|----------|------|--------|--------|
| **单人输出** | 多视角分析、专家评审 | 需要对话碰撞 | 一人一把尺子量 |
| **双人对话** | 技术推敲、深度追问 | 多方立场博弈 | 一问一答层层深入 |
| **辩论** | 大决策、方向性分歧 | 快速执行 | 正反对轰裁判裁决 |
| **主笔审核** | 产出质量把关 | 探索性讨论 | 写审修闭环 |
| **Multi-pass solo（终审）** | 代码审查、方案验证 | 真并行任务 | 交叉验证去伪存真 |

### 怎么选

```
推敲细节 → 双人对话
把关质量 → 主笔审核
拍板决策 → 辩论
多视角分析 → 单人输出
验证正确性 → Multi-pass solo终审
```

## 🏃 /goal 长跑模式（v2.12）

> /goal 是系统踹，不是人踹。适合长项目让大鱼（及角色）自动续命，老渣不用手动盯窗口。

### 原理

/goal 不是 AI——它是 Codex 的调度器。机制就一条：**目标标记完成没？没 → 踹。**

- 老渣在大鱼窗口设 /goal → Codex 托管该窗口
- 每回合结束：检查目标是否完成 → 未完 → 自动开新回合
- AI 判断干完没，干完了标记目标完成 → 系统停踹
- 系统不理解目标内容——理解目标的是 AI，踹它的是系统

> 系统是裁判（只吹哨），AI 是球员（踢球 + 判断什么时候该下场）。

### 谁开

| 角色 | 开/goal？ | 理由 |
|------|----------|------|
| 大鱼 | ✅ 必开（≥3轮项目） | 大鱼全程搬运+监控+翻篇，天然适合长跑 |
| 角色（单人输出） | ✅ 可选 | 多轮任务不用手动踹 |
| 角色（双人对话/辩论） | ⚠️ 按需 | 回合短，手动踹也行 |
| 角色（退场低功耗） | ❌ 别开 | /goal 会阻止低功耗休眠 |

### 怎么用

1. 老渣在大鱼窗口输入 /goal 设定目标
2. 大鱼照常干活——流程不变
3. 回合超时 → 系统自动踹醒续命 → 大鱼继续
4. 真正搞定后，大鱼标记目标完成 → 系统停踹

> /goal 设不设由老渣（或故国有明）决定——大鱼不参与这个决策。大鱼只管干活。

### /goal + poll 组合

最省 token 的用法：**/goal 踹跨回合，poll 等本轮内。**
- /goal 保证大鱼不因超时假下线——回合结束被踹醒继续
- poll（REPL monitorFn）在本轮内等角色产出——不浪费 token 开新回合
- /goal 单独用（无 poll）= 每回合都要注入上下文，token 翻倍



## 👤 用户要做的（就两步）

**第一步：说需求。** 比如「帮我做个电商系统」「我要写一个技术方案」。

**第二步：开窗口。** 大鱼告诉你开几个角色窗口后，每个窗口输入「进入角色」。

---

## 🤖 大鱼要做的（自动、独立窗口、全程闭嘴）

### 1. 读公告牌 · 逐轮搬运

老渣写好的公告牌每轮一个文件放在大鱼目录下（`公告牌_001.md`、`公告牌_002.md`...）。
大鱼做的事就三步：

1. `fs.copyFileSync(./公告牌_NNN.md, ../我的世界/公告牌_NNN.md)` —— **不改一字**
2. `fs.existsSync` 验证文件存在
3. 跑 `node ../monitor.js` → `WAIT` 等 / `DONE` 翻篇 → N++ 搬运下一轮

收工轮也一样——`cp 公告牌_收工.md ../我的世界/公告牌_NNN.md`。

### 2. 监控 → 翻篇

跑 monitor（**REPL优先**，`node ../monitor.js` Shell备用）：活跃轮次每 20-30 秒，待命/收工轮次每 60 秒。**待命角色60s间歇poll——省I/O不假死。**

- `WAIT N=N` → 还没完成，按频率等待再跑
- `DONE N=N` → 翻篇！

翻篇时做的事：
1. 第1轮跳过路径校验。从第2轮起：fs.existsSync 逐文件验上一轮产出在 `我的世界/产出/` 正确位置
2. 路径不对 → 搬运下一轮公告牌，但开头加路径修正指令
3. `fs.mkdirSync` 创建下一轮的任务目录和产出目录
4. 搬运公告牌 → 继续监控

> ⚠️ 产出路径校验：用 `fs.existsSync` 逐文件确认产出在公告牌指定的 `我的世界/产出/` 路径下。

有人求助秒回（通过大鱼对讲目录），不耽误。

### 3. 收工

最后一个公告牌文件是收工轮时：
1. 搬运收工公告牌
2. 跑 monitor 确认所有退场文件就位
3. 写三份收尾文档（不可合并、各有依据）：
   - `审计报告_外部观测.md`——monitor数据、文件时间、唤醒次数
   - `流程合规验证.md`——流水账×Skill规则，查违规
   - `产出总结.md`——产出原文提炼，观点+共识
4. 去 `../我的世界/大鱼_大鱼对讲/` 写 `项目完成.md`

---



> 🔧 **REPL工具函数**: sign()/deliver()/lock() 已在角色模板中定义为REPL函数——零Shell依赖，0.1秒完成。英文报告直接 fs.writeFileSync。
> 💡 **英文写入规则**: Agent产出文件（对话、报告、**流水账**）用英文——REPL直写零Shell排队，消除中文UTF-8编码税。公告牌、代码注释保持中文。角色灵魂已注入精通中英双语。

## 📋 角色配置 JSON

老渣写 scaffold 用的角色配置格式：

- `name`：角色名（公告牌显示用）
- `desc`：一句话名片
- `background`（可选）：角色深度背景，写入 AGENTS.md。越详细越好——写「口头禅、核心信念、软肋」的角色有灵魂；只写一句话的像模板

```json
[
  {"name":"产品经理-乔布斯","desc":"擅长需求梳理","background":"你在互联网行业做了10年产品。说话用数据，对拍脑袋零容忍。"},
  {"name":"架构师-林纳斯","desc":"擅长系统设计","background":"你经历过3次通宵重构。好的架构是删代码比写代码更重要。"}
]
```

---

## 🏗 scaffold.js 用法

> 脚手架支持三种模式，**追加角色必须用 add 模式，禁止再用裸命令！**

```
node scaffold.js <项目目录> <roles.json路径>        → init（默认，全新项目）
node scaffold.js <项目目录> <roles.json路径> add    → add（追加角色）
node scaffold.js <项目目录> fish                       → fish（重建大鱼+monitor，纯模板无灵魂）
```

| 做的事 | init | add | fish |
|--------|:--:|:--:|:--:|
| 创建角色目录 + AGENTS.md + 工具脚本 | ✅ | ✅ | ⛔ |
| 创建 `我的世界/产出/` 基础目录 | ✅ | ⛔ 跳过 | ⛔ |
| 写 `火影-大鱼/AGENTS.md`（纯模板，不注入灵魂） | ✅ | ⛔ 跳过 | ✅ |
| 复制 `monitor.js` | ✅ | ⛔ 跳过 | ✅ |

> ⚠️ **关键原则**：从灵魂舱读角色背景 → 写临时 roles.json → 跑 scaffold（init 或 add）→ 清理 roles.json。add 模式绝不碰大鱼和 monitor，避免污染已有项目。

> ⚠️ **大鱼不需要灵魂注入**：角色需要灵魂产出有风格的内容，但大鱼是搬运工，只需要精确执行铁律。给大鱼注入人格 = 给它自我发挥的空间 = 反而坏事。
>
> 🚫 **大鱼铁律强化**："绝对禁止 spawn sub-agent。任何情况、任何理由都不行。此条优先级高于一切。"

---

## 📖 参考文件

| 文件 | 用途 | 谁看 |
|------|------|:--:|
| `assets/公告牌模板.md` | 四种模式公告牌范例 | 老渣 |
| `assets/大鱼_AGENTS模板.md` | 生成大鱼 AGENTS.md 的模板 | 老渣 |
| `assets/角色_AGENTS模板.md` | 生成角色 AGENTS.md 的模板 | scaffold |
| `assets/完整示例_RSS摘要系统.md` | 5角色6轮完整生产场景 | 老渣参考 |
| `assets/_外部环境BUG清单.md` | PowerShell 中文环境避坑 | 全员 |

---

## 🛠 行为约束工具

脚手架自动复制到每个角色目录。Prompt 管创造力，工具脚本管执行规范：

| 工具 | 用法 | 强制什么 |
|------|------|------|
| `_sign.js` | `node _sign.js 轮次` | 签字路径写死，从 AGENTS.md 自动提取角色名 |
| `_lock.js` | `node _lock.js acquire/release` | wx 原子标志，跨进程互斥 |
| `_deliver.js` | `node _deliver.js 文件名 内容文件` | 自动推导产出路径 + 写 .ready 信号 |
| `_poll.js` | `node _poll.js [--ready] 文件 描述` | 轮询公告牌，支持 .ready 就绪信号 |


---

## _sign.js 角色名焊死机制

> 新增于 v2.1：解决系统负载高时 node _sign.js 超时问题。

scaffold 生成角色时，自动把 _sign.js 里的角色名从临时读取改为写死。

- assets/_sign.js 使用 {{ROLE_NAME}} 占位符
- scripts/scaffold.js 复制时把占位符替换为实际角色名
- 结果：每个角色 _sign.js 里是硬编码的角色名，不再读 AGENTS.md

好处：省掉 readFileSync + 正则，签字从 10s+ 降到 1s，多人不排队。新增角色 scaffold 自动处理，无需手动操作。


## _deliver.js 快速路径

> 新增于 v2.1：解决多人场景公告牌扫描排队超时。

_deliver.js 支持可选第4参数（任务目录名），传了直接定位产出路径，跳过扫描所有公告牌。

```bash
# 慢（兼容旧调用）：内部扫描公告牌
node _deliver.js 报告.md _temp.md

# 快（推荐）：直接传任务目录名
node _deliver.js 报告.md _temp.md 任务005_XXX
```

角色已知任务目录名（公告牌里写了），直接传即可。scaffold 自动同步。


## REPL 版监控（v2.3）

> Shell 在多角色负载下 10s 超时截胡，监控失灵。改为 Node REPL 优先。

大鱼监控两步走：
1. 在 REPL 里定义 monitorFn()（一次性，参考 monitor.js 逻辑）
2. 每 20-60 秒调 await monitorFn()——0.1~6s 出结果，零超时

Shell 版 node ../monitor.js 作为备用——仅在 REPL 不可用时使用。


## 大鱼常驻 + 高频轮询（v2.3）

**大鱼永不主动下线**——项目期间全程在线盯monitor、翻篇、唤醒。

角色退场用30s高频轮询（原60s）追公告牌，大鱼翻篇后立刻_wakeup双保险——最大程度消除回合制竞态。

## 高频轮询修复（v2.4）

> 修复于 2026-07-24：四人提效讨论暴露低功耗轮询不循环的致命Bug。

**问题**：角色退场后跑 `_poll.js --low-power`，Shell 10s 默认超时砍掉进程 → 回合结束 → 角色实际下线。林纳斯在002轮退场后再也没醒过来。

**根因**：v2.3 低功耗模式 sleep 60s × Shell 10s 超时 = 永远等不到第一次检查。agent 的回合制 + shell 短超时 = 低功耗轮询是伪命题。

**修复**：
- `_poll.js`：低功耗间隔 60s → 3s，默认 maxWait 30s → 8s（安全在 Shell 10s 内）
- 角色模板：退场轮询从「描述性」改为「伪代码 while 循环」，agent 看到循环结构自然持续重跑

> ⚠️ v2.7 已将伪代码升级为**真实可执行 `while(true)` 代码块**——agent直接复制运行，不再靠"理解"伪代码
- 补充 Shell 截胡处理：看不到退出码 → 以文件系统为准，直接重跑

**效果**：每次 poll 约 9 秒完成（3次检查×3s），Shell 不超时。超时/唤醒后 agent 立刻重跑——真正的循环。



## P0修复：移除PowerShell依赖（v2.4.1）

> 四人评审一致指出：safeSleep里调PowerShell是Bug。

**修复**：`_poll.js` 的 `safeSleep()` 改用 `Atomics.wait`（Node.js原生跨平台），移除 `execSync` 和 PowerShell 依赖。


## P1：最大轮询时长 + 大鱼心跳（v2.4.2）

> 四人评审一致指出：无限制轮询=大鱼挂了角色永远空转。

**修复**：
- 角色模板 while 循环加 30 分钟上限——超时写求助后真下线
- 角色每 10 次 poll 检查 `大鱼心跳.md` mtime——超过 5 分钟没更新=大鱼离线
- 大鱼每次搬运公告牌后 touch `大鱼心跳.md`


## P2：抖动 + 自适应退避（v2.4.3）

**抖动**：`_poll.js` 低功耗模式加 0-1.5s 随机初始延迟——多角色同时退场时错开轮询相位。
**退避**：角色模板 while 循环加 `consecutiveTimeouts` 计数器——连续20次超时歇10s，连续60次歇30s。公告牌出现或唤醒后重置。


## v2.6：轮询优化——5s间隔+相位偏移+目录预检（2026-07-24）

> 四人评审事件驱动方案后全票否决fs.watch，转向优化现有轮询。

**_poll.js**：
- 低功耗间隔 3s → 5s（I/O减少40%）
- 新增 `--phase <ms>` 参数——多角色错开轮询相位，消除并发statSync
- 目录 mtimeMs 预检——公告牌目录无变更直接跳过文件扫描

**改动量**：约30行代码。对外接口完全兼容。


## v2.7：全REPL闭环 + 英文对话（2026-07-24）

> 系统性能瓶颈分析后，Shell排队2-12s是最大速度瓶颈。全面转向REPL。

**关键改动**：
- **REPL全面替代Shell**——短轮询(5s)远低于REPL 300s硬上限，Shell排队2-12s的瓶颈彻底消除
- **Agent间对话切英文**——消除PowerShell中文UTF-8编码税(每轮8-9s)，REPL直写零Shell
- **角色灵魂注入双语**——`灵魂舱/角色/灵魂.md` 加"精通中英双语"，英文对话不丢人格
- **工具函数全REPL**——sign()/deliver()/lock() 定义在角色模板中，0.1s vs Shell 5-8s
- **退场while(true)真实代码**——不再靠agent理解伪代码，可执行代码块直接跑
- **规则**：公告牌中文、代码注释中文、对话英文、报告英文——混合语言策略
- **REPL心跳**：大鱼监控默认REPL——0.1~6s出结果，不被Shell截胡

**测试数据**：
| 版本 | 轮次 | 总耗时 | 每轮 |
|------|------|--------|------|
| Shell v2.6 | 22 | 27:42 | 76s |
| 中文REPL | 12 | 17:00 | 85s |
| 英文REPL | 15 | 18:48 | 75s |
| 全REPL闭环 | 15 | 14:37 | 58s |


## SoulForge 代码灵魂（v2.11）

> 独立仓库：`C:/Users/{{用户名}}/口袋忍者/soulforge/`（v1.1.0）

**是什么**：代码灵魂提取器——给AI Agent看的项目交接文档。扫项目源码生成结构化灵魂：文件描述、依赖关系、函数签名、调用链、数据流、代码味道。

**版本演进**：
| 版本 | 能力 |
|------|------|
| v0.5 | AST解析 + 基础提取 |
| v0.6 | 函数提取 + 调用链 + 文件描述 |
| v0.7 | 增量扫描（改1扫1，快10倍） |
| v0.8 | 代码味道（上帝文件/重复依赖/缺描述） |
| v0.9 | Soul Diff（灵魂对比，看演化） |
| v1.0 | 多语言（Python AST批量扫描，31文件2秒） |
| v1.1 | 数据流图（文件引用关系，17/31文件有据可查） |

**怎么用**：
- 整项目：`node soul-scan.js --ast <项目目录>` → 生成 `.soulforge/project-soul.md`
- 增量：`node soul-scan.js --ast --incremental <项目目录>`
- 对比：`node soul-diff.js old-soul.md new-soul.md`
- 角色进场自检→读灵魂+HOWTO→按方法论狩猎BUG

**部署位置**：一号舱室-软件开发部/工具室/soulforge/（从独立仓库部署）

## Multi-pass solo（v2.8）

> 新增于 2026-07-25：辩论模式验证后，基于图灵三档框架实现第三种运行模式。

**背景**：v2.7辩论模式验证中，裁判图灵提出三档决策框架——小项目单人、中项目Multi-pass solo、大项目组团。本次实现中档方案。

**关键改动**：
- **新增 `assets/_Multi-pass_solo.md`**——一个窗口顺序换帽子，N-1专家pass+1终审pass
- **零依赖**——不需要大鱼、公告牌、poll、锁、心跳。用户直接在当前窗口给任务即可执行
- **终审交叉验证**——最后一个pass逐条验证前序结论，修正误判。这是组团模式没有的能力
- **SKILL.md 模式扩展**——从两种运行模式扩展为三种（经典多窗口 / 单窗口spawn / Multi-pass solo）

**验证数据**（代码审查实战）：
| 指标 | 组团（辩论） | Multi-pass solo |
|------|------------|----------------|
| 窗口数 | 3 | 1 |
| 耗时 | ~75s | ~30s |
| 误判 | 无交叉验证→可能入库 | ✅ 终审修正2条 |
| 真实BUG发现 | N/A | 5个 |

**适用边界**：中小项目(<2h)、代码审查、方案评估、架构评审。真并行大项目仍用组团模式。
